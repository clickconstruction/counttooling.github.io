# ClickCount — Agent Instructions

## Spec Reference

**RECONSTITUTE.md** (if present) is the base spec for behavior and data structures; otherwise see ARCHITECTURE.md. The app includes many extensions; see ARCHITECTURE.md "Features Beyond Spec" for the full list.

## Tech Constraints

- Vanilla HTML, CSS, JavaScript
- No build step; static deployment
- Single-file architecture: HTML + CSS + JS in `index.html`
- report.js loads after index.html and uses globals: `state`, `makeAnnotations`, `ptDist`, `polylineDistance`, `formatDist`, `renderIconHtml`, `quickLineLength`, `getLineLengthPdfPts`, `getLineLengthForTotals`, `getLineRealWorldLength`, `getMultiplyZoneForLine`, `getMergedAnnotationsForPage`; exposes `buildReportHtml`, `printReport`, `getPipeToolingSummary`, `getEmailTextSummary`; both summary functions accept optional `options` with `pageIndices` and `getAnnotations`
- jsPDF for Export PDF; html2canvas for report-to-PDF (Export PDFs)
- Supabase (optional): Auth, projects table (`pdf_path`, `pdf_hash`, `size_bytes`), `pdfs` storage bucket, RPC `list_users_for_admin`, `list_projects_for_admin`, `list_user_activity_for_admin`, `list_user_activity_summary_for_admin`; Edge Functions (`admin-create-user`, `admin-delete-user`, `admin-delete-project`, `admin-list-users`, `invite-to-project`, `get-view-project`); config via `config.js` (see SUPABASE_SETUP.md)
- **Supabase migrations**: When creating or modifying migrations in `supabase/migrations/`, always apply them via the Supabase MCP `apply_migration` tool (name: filename without .sql, query: file contents)

## Navigation

1. **Read ARCHITECTURE.md first** — Contains line ranges, section map, and feature list for index.html
2. **Use grep/semantic search** — For specific features, use the search hints in ARCHITECTURE.md
3. **Prefer targeted reads** — Use `read` with offset/limit when editing a known section instead of loading the full file

## Conventions

- Preserve existing patterns and structure
- Coordinates: annotations in PDF-space; use `canvasToPdf` / `toCanvas` for conversion (toCanvas includes devicePixelRatio)
- Do not remove or rename globals used by report.js
- **Scale is per-page**: `page.scale`; use `getPageScale(pageIdx)` to read; never use `state.scale`
- **Persisted settings** (localStorage): `loadProjectFiltersExpanded` (Load Project modal: filter strip expanded vs search-only); `counterSettings`, `lineTypeSettings` (includes `parallelEndsSize`, `lengthLabelSize`, `snapToHorizontalVertical`, `showOnlyLinesOnCurrentPage`), `legendSettings` (bgOpacity, textOpacity, bgColor, showBorder, legendScale, showResizeHighlight), `multiplyZoneSettings` (showLabelOnZone, defaultMultiplier, labelSize, labelPosition), `gridSettings` (spacing, unit, offsetX, offsetY, opacity, color, lineWidth, lineStyle, majorInterval?, snapToGrid), `showGridOverlay`, `exportSettings`, `recentLineColors`, `iconNames`, `iconOrder`, `customIconPaths` (IndexedDB; in-memory cache, loaded at init; one-time migration from localStorage), `pageScales`, `zoomSettings`, `groupColorDisplay`, `pagesTitlesTruncated`, `hideUnmarkedPagesFromSidebar`, `counterSearch`, `lineTypeSearch`, `linesSearch`, `linesTypeExpanded`, `plumbingModifiers` (includes `iconByType` for type-to-icon mapping; also in user_airboard when Artboard saved), `lineModifiers` (Quick Line Size/Material; also in user_airboard when Artboard saved), `specificPagesIncludeReport`, `bundleHighlightsToPdf`, `bundleNotesToPdf` (in exportSettings), `clickcount-last-project` (last-project restore metadata); `clickcount-last-global-reload` (stamp written by `doGlobalReloadNow` before `location.reload()` to prevent reload loops; compared against `system_settings.force_reload_after` on boot via `checkGlobalForceReload`); `clickcount-debug-save` (Verbose mode toggle in the Save Status modal — `setSaveDebugEnabled` writes `'1'` to enable, removes the key to disable; while on, `saveDebugLog` tees console-only diagnostics into `saveStatusLog` as kind `debug` and the rolling window extends from 5 min to 60 min); `maxZoom` per project in save/load; `groups` per project in save/load; `activeCanvasIdByPage` per project in save/load; include new fields in export/import when adding; `config.example.js` as template; `config.js` committed for production deployment. In-memory (not persisted): `state.pdfBufferSize` (bytes; set when `state.pdfBuffer` is set; used because pdf.js detaches the buffer, making `byteLength` 0); `state.userActivityAllRowsCache`, `state.userActivityViewMode` (admin User Activity modal)
- **Line color modal**: `showLineColorModal(currentColor, onApply)` — used for Counters, Line Types, Groups, Lines; Choose Color modal: Presets (COLORS swatches), color picker, Recent; `applyLineColor(color)`; `state.recentLineColors`; `state.pendingLineColorApply`
- **Supabase**: When enabled, `state.supabaseSession`, `state.isAdmin`, `state.currentProjectId`, `state.currentProjectName`; cloud features hidden when `SUPABASE_ENABLED` is false; PDF uploads limited to 50 MB (Supabase storage limit)
- **Modals**: Project Settings (gear, top right on desktop) — Name / Upload / Save Project to Cloud, Load Project from Cloud, Close Project (hidden when no project), Check out Project / Turn In Project / Force Turn In (admin only for force); **Save Status** — two bells open `saveStatusModal`: in-modal bell (`saveStatusBtn`) in the Project Settings checkout row (visible whenever the checkout section is visible and signed in with a project), and header bell (`saveStatusBtnHeader`, class `header-save-status-btn`) in the header (visible whenever signed in to Supabase; hidden on mobile via CSS); both default gray and add `save-status-bell-attention` (yellow) when `lastCloudSaveAttemptFailed && autoSaveDirty` or `checkoutExpiredNeedsAttention`; modal shows canvas + PDF cloud summary at top and rolling 5-minute `saveStatusLog` activity (60 minutes while Verbose mode is on); while open, `saveStatusModalTickTimer` re-renders content every 5s via `renderSaveStatusModalContent` so "Xs/Xm ago" labels stay live (cleared on close or when modal is no longer `.visible`); modal actions row exposes **Verbose mode** checkbox (`#saveStatusVerboseToggle`, persisted via `setSaveDebugEnabled`/`localStorage.clickcount-debug-save`; tees `saveDebugLog` into `saveStatusLog` as kind `debug` with 4 KB detail cap; flips activity heading to "Activity (last 60 minutes)"), **Copy logs** (`navigator.clipboard.writeText` of `buildSaveLogsEnvelope()` JSON with hidden-textarea + `execCommand` fallback), and **Export logs** (`clickcount-save-logs-{ISO}.json` downloaded via Blob + `URL.createObjectURL`); envelope schema `clickcount-save-logs/v1` carries `{capturedAt, user, browser{ua,platform,onLine,network from captureNetworkInfoDetail}, timing{lastSuccessfulSupabaseCallAt, serverClockOffsetMs, consecutiveAutoSaveFailures, autoSaveDirty, saveInProgress, turnInInProgress, verbose, windowMs}, project: getProjectSummaryForLogs() counts-only summary, events: saveStatusLog.slice()}`; autosave/auth sync failures use Save Status, not generic sync toast; edit-session expiry (`CHECKOUT_EXPIRED`, `pushSaveEvent('checkout_expired')`) uses Save Status, not toast; `clearCheckoutExpiredAttention()` on successful Check out or Turn in, Share (add users by email), Add additional PDF pages (when PDF uploaded), Download PDF (when PDF in project), Manage Projects (admin), Clear Page (when PDF), Macros (yellow link, below Clear Page, above Advanced), Advanced section (collapsed by default) with Load test PDF (localhost only), Manage Icons, Export Canvas (when markup, editor only), Export PDF (when PDF in project; both available in view mode), Import Canvas (hidden in view mode), Canvas Repair (when PDF), Empty cache and hard reload (red danger; clears IndexedDB and localStorage, reloads), Global force reload (admin) (`advancedGlobalForceReload`; calls `admin_trigger_global_reload(p_reason)` RPC from migration 041; on UPDATE of `system_settings.force_reload_after`, every signed-in tab fires the `system-settings-changes` realtime channel and shows the sticky top `#globalReloadBanner` with manual Reload + Dismiss; boot-time `checkGlobalForceReload` compares server `value_ts` to `localStorage.clickcount-last-global-reload` and triggers `doGlobalReloadNow` (writes stamp BEFORE `location.reload()`, deletes `clickcount-pdf-cache` IndexedDB and the same localStorage key list as Empty cache); view-link anonymous users skipped (no session)); User Settings (user icon) — email, password, Artboard (Save, Load from Cloud, Export, Clear Artboard; counters, line types, plumbingModifiers, lineModifiers to user profile); mySettingsClearAirboard, Add User / Manage User (admin), All Users (admin), Sign Out; Manage User modal — header with yellow activity icon (`manageUserModalAllActivityBtn`) opening **User Activity** in all-users mode; list users with Delete per row; per-row activity icon opens single-user activity; Manage Projects modal — list all projects with Delete per row and Force turn-in (admin) per checked-out row; project name on top, date/time/size below, "Canvas only" badge for projects without PDF; **User Activity modal** (`userActivityModal`) — admin; **Events** vs **Summary** (all-users mode); Events: `list_user_activity_for_admin` + `list_users_for_admin`, toolbar (user dropdown, filter on `state.userActivityAllRowsCache`), `state.userActivityViewMode`; event **Time** and single-user rows use `formatUserActivityDateTime` (**America/Chicago**); Summary: `list_user_activity_summary_for_admin`, last sign-in via `formatLastSignInUserActivity` (calendar Today/Yesterday in Chicago); Manage/All Users lists still use `formatLastSignIn` (browser-local); groupAssignModal (Assign to group) with + Add group opening groupModal (Add/Edit Group; z-index 210 so it stacks above); Export PDFs modal — thumbnails, per-page marked/unmarked/exclude, bulk actions, Include takeoff report / Bundle highlights / Bundle notes toggle switches with "— none to show" and disabled when no data; loadAnnotationsModal — "You have saved annotations for this PDF. Load them?" with project list; shown on PDF upload when hash matches cloud projects, gated by `!state.currentProjectId` so a project already loaded can never be silently switched; row-click now fetches the full row via `list_accessible_projects` and calls the shared `hydrateProjectFromCloudRow(proj, opts)` helper with `{reusePdfHash: uploadHash, reusePdfStoragePath: proj.pdf_path}` (so checkout / permissions / realtime / `clickcount-last-project` hydrate identically to Load Project and the just-uploaded PDF is reused without a wasted re-upload); a page-count mismatch confirm runs before applying annotations and `state.activeCanvasIdByPage` is sanitized to indices present in the current PDF; canvasOnlyNeedsPdfModal — "This project has annotations but no PDF" / "This project's PDF is missing"; opened via `openCanvasOnlyNeedsPdfModal({reason?})` after loading a canvas-only project (replaces the old toast + auto `pdfInput.click()` pair); actions: "Choose PDF..." triggers `pdfInput.click()`, "Skip for now" closes the modal; a persistent `#canvasOnlyNeedsPdfBanner` (same `.sync-paused-banner` styling, yellow) shows in the status row whenever `state.currentProjectId && state.pages.length === 0 && state.pendingCanvasLoad && !state.isViewer` and the modal is hidden, so dismissing the modal isn't a dead end; banner state refreshes from `updateUI()` every render via `updateCanvasOnlyNeedsPdfBanner()`; viewLinkEmailModal — "Enter your email to view these plans" for view-only share links; saveBeforeLoadModal — unsaved changes prompt (Save now / Don't Save / Cancel); lastSessionRestoreModal — on boot when `last.projectId` exists (no longer requires `last.pdfHash`); local backup checked first: when IndexedDB has backup under `'local'` with pdfBlob and data, offers "You have a local session from your last visit" (Load test PDF → Open without save); cloud project restore offered when no local backup; for local restore, proj.id is `'local'`, `state.currentProjectId` set to null, `cachedBlob` passed from backup; uses `proj.pdf_hash || last.pdfHash` for cache lookup; Supabase restore uses `download()` with empty-blob check; `hasIdbPdf` requires `pdfBlob.size > 0`; prompts Keep / Discard before restore; Discard clears local state and deletes project from cloud if owner; saveProjectModal — contents list shows Canvas (included) and PDF with size in MB; size from `state.pdfBufferSize` (local) or pdfCache/storage.info (cloud); fallback to PDF section when `state.pages.length > 0`; "Canvas only" when no PDF; pipeToolingCopiedModal — "Copied to clipboard" toast (1.5s auto-dismiss); noteModal — Add/Edit Note (textarea, Cancel/Done); multiplyZoneModal — Multiply Zone: preview "In this area: X counter(s), Y line run(s) (Z ft)", multiplier input (min 1, default 2), Cancel/Apply; deleteZoneModal — Delete Zone: preview "In this area: X counter(s), Y line run(s), …", Cancel/Delete (confirm before delete); multiplyZoneSettingsModal — Multiply Zone Settings: Show label on zones toggle, Default multiplier, Label size (8–24), Label position; opened by right-clicking multiply zone icon in header or sidebar; openMultiplyZoneSettingsModal; scaleModal — tabs Select two points, Architectural & Engineering (presets); Custom scale at bottom (fraction e.g. 1/4 or 0.25, feet, Apply); when `scaleModalApplyTarget === 'zone'` (after drawing a Scale Zone rectangle or context **Edit scale**), Apply writes `ann.scaleZones[].scale` instead of `page.scale`; `pendingScaleZone`, `pendingScaleZoneEdit`; `resetScaleModalZoneMode`; full-width; parseFraction; chooseLineTypeModal — tabs Choose | Create | Quick (like Counter modal); search; Shift+L for Quick tab; L always opens modal; Snap-to-H/V toggle in header (right of Polyline button; visible when Line or Polyline tool selected); also in Line Type Settings; J hotkey; macrosModal — Keyboard Shortcuts (M/S/C/L/J/P/D/H/X/N/R/Esc/arrows/Enter/Ctrl+Z/Ctrl+Shift+Z/Ctrl+R; Shift+C Quick tab when Counter modal open; Shift+L Quick tab when Line modal open; X = Multiply Zone; Scale Zone = rotated Set Scale icon in header/sidebar), opened via Project Settings Macros link; customIconTipsModal — Custom Icon Tips (what makes a great icon for upload); opened by clicking "Custom Icons" label in Create Counter or Counter Details; counterLineTypeDetailsModal — opened via edit pen on Counter or Line Type row; Name (editable), Color (swatch), On pages (clickable jump), Delete (count=0 immediate; count>0 confirm + remove markers/lines); linePropertiesModal — Line Properties: read-only **Line type:** line (`#linePropertiesLineType`, centered; from `state.lineTypes` / `lineTypeId`, or em dash if missing); Name, Color, Start drop, End drop, +1/+10/-10/-1/Clear buttons, Edit vertices for polylines; opened via edit pen on individual line in Lines dropdown or context menu "Line Properties"; plumModal — Quick Plumbing: PLUM buttons hidden in Counters and Lines sections (`.sidebar-plum-row { display: none }`); plumModal still exists for programmatic use; Name at top, Add Counter in header right; Size, Type (icon box for type-to-icon mapping), Material; Icon tabs (Icon/Custom Icons); 3 rows visible in icon grid; `getPlumbingModifiers`, `savePlumbingModifiers`, `PLUMBING_DEFAULTS` (sizes 0.5in format); summaryCountDetailModal — "— by page" breakdown with thumbnails when clicking count/line in sidebar Summary; legendSettingsModal — Summary Legend: background opacity, text opacity, background color, Show border, Legend size (50–400%), Highlight resize area; opened by clicking Summary header; gridSettingsModal — Grid overlay: spacing, unit (ft/in/m/cm/yd); origin row (Set origin on page, Snap counters to grid toggle); Line color, Major line every N lines; Line opacity and Line width in one row; line style (solid/dashed/dotted) centered; opened when grid button clicked and grid is off; Apply enables grid; `resetGridOrigin()` on new document load; drawGrid draws lines left/above and right/below origin; zoomModal — Zoom Settings (max zoom per project, mousewheel speed universal), opened via zoom percentage click; Manage Icons — Custom icons section with Edit (toggle edit mode), select rows, Delete selected; preparePdfModal — Prepare PDF for Cloud (Add additional PDF pages): Project name, Page name (collapsible dropdown minimized by default, icon ▶ at end of "Page name", whole line clickable), Prev/Next, Delete, Rotate (90° per page), Undo; left: Cancel, Open; right: Download Trimmed PDF, Save and Open; Open (left) runs and awaits `writeTakeoffStateBackup()` immediately after commit; per-page file size (This page / Total) when computed; commitPreparePdfToState; `openPreparePdfModal(pages, buffer, defaultName, opts)` accepts `{mode: 'project'|'append'}` — append mode (used by Project Settings "Add additional PDF pages") swaps the title to "Add pages — {currentProjectName}", swaps the description, hides `#preparePdfNameRow`, and in `commitPreparePdfToState` merges the trimmed buffer onto `state.pdfBuffer` via `mergePdfBuffers`, appends new entries onto `state.pages`, re-binds the existing `state.pages[i].pdfPage` refs to the merged document so a single pdfjs document backs every page, enforces the 50 MB ceiling on both the projected (existing + new) and merged byte counts via `assertPdfWithinLimit`, preserves `state.currentProjectId/Name`, and resets `state.pdfStoragePath = null` + `state.pdfHash = null` so the next save re-uploads; if `state.pdfBuffer` is missing at commit time the append branch returns an error instead of silently replacing the project's PDF; the Project Settings "Add additional PDF pages" button (`settingsAddAdditionalPages`) recovers `state.pdfBuffer` from `pdfCacheGet(state.currentProjectId, state.pdfHash)` when null, then sets module flag `pendingAddAdditionalPages = true` and clicks `#pdfInput`; `pdfInput.onchange` captures and clears the flag at the very top, and when set with `state.currentProjectId && state.pages.length > 0` it reads + (optionally) merges the uploaded file(s) into a single new buffer and calls `openPreparePdfModal(..., {mode:'append'})` instead of falling through to the direct-append code path; `loadTestPdf` is gated on `!state.currentProjectId` so the dev fixture can no longer wipe a loaded project's name/buffer; `hydrateProjectFromCloudRow(proj, opts)` is the shared helper (declared near `resolvePdfBufferForCloudProject`) that owns `state.currentProjectId/Name`, `state.pdfHash`/`state.pdfStoragePath` (only when `opts.reusePdfStoragePath !== undefined`), `lastSaveIncludedPdf`, `state.lastSavedAt`, `lastLocalBackupAt`, `state.currentPage`, `autoSaveDirty/lastModifiedAt`, `state.checkedOutBy/At/Email`, `state.loadedViaViewLink`, `state.isViewer`, `state.canCheckOut`, `state.projectOwnerId`, `subscribeToProjectCheckoutChanges`, `clearCheckoutExpiredAttention`, `logProjectOpenEvent`, and `clickcount-last-project` persistence; opts: `{reusePdfHash?, reusePdfStoragePath?, source?: 'load_project'|'load_annotations'|'restore_last'}`; the helper always clears `state.pendingCanvasLoad` at entry, and the Load Project modal row-click also clears it before any other state writes (so a canvas-only A → dismissed picker → load B sequence cannot apply A's pending data to B); doRestoreLastProject also clears pendingCanvasLoad at entry; authModal — Sign In with "Sign in as test user" (localhost only, when DEV_AUTH_EMAIL/DEV_AUTH_PASSWORD in config)
- **Summary legend overlay** — `state.showLegendOverlay` (default true); toggled via legend button in header/sidebar; overlay on canvas with counters and line totals; draggable (header bar); resizable from bottom-right grip (16×16 PDF-unit hit area); Legend Settings modal for appearance; legend in annotations: `{ x, y, w, h, userResized? }`; `userResized` set when user manually resizes (drawLegend preserves size); resize/drag use window mousemove and mouseup so drag continues when cursor leaves canvas; mouseleave does not clear `resizingLegend`/`draggingLegend`
- **Export dropdown** — `#exportDropdown` in header; 28×28 cloud icons (aligned with gear, Share, print); cloud-upload when editor has no pages (click fires `#pdfInput` — no import submenu); cloud-download opens export menu when pages exist (Export Canvas/Both gated by `projectHasAnyCanvasMarkup()`, Export PDF when PDF in project; **Import Canvas** in menu when editor, PDF, no markup, opens `#importInput`; menu hidden when no rows); Advanced **Export Canvas** and sidebar Import still available; `#exportBtn` no-ops without markup; viewer with no pages: control hidden
- **Export Options** — Yellow section title above Export PDFs in sidebar (`#exportOptionsSectionTitle`); Copy to /Tooling is the yellow (primary) button; Copy Summary (Email/Text) dropdown (`#copySummaryTextDropdown`) with same canvas options; both dropdowns: This Canvas Only (hidden when single page), All Visible Canvases, All Canvases; Copy Summary menu pops up when no space below
- **Download current page** — `#downloadCurrentPageBtn` in header (far right); yellow printer icon (Font Awesome–style SVG, 28×28); dropdown when multiple canvases or multiple pages: "Print This Canvas Only" (active canvas), "Print All Canvases on Page" (one PDF page per canvas with caption), "Print All Plan Pages and their Current Canvas" (all pages, active canvas each), "Print All Pages & Canvases" (all pages, all canvases with captions); single page and single canvas: direct download; visible in edit and view mode when PDF loaded; `downloadCurrentPageAsPdf(mode)`; uses `renderAnnotationsToContext`, jsPDF; filenames `takeoff-page{N}_{projectName}.pdf`, `takeoff-page{N}_all-canvases_{projectName}.pdf`, `takeoff-all-pages_{projectName}.pdf`, `takeoff-all-pages-canvases_{projectName}.pdf`
- **Download PDF** — Project Settings "Download PDF" downloads the current project's PDF as-is; Prepare PDF modal "Download Trimmed PDF" (right side) downloads the trimmed PDF (kept pages only). Rotation applied in Prepare PDF is stored in state.pages on commit; `downloadProjectPdf()`, `downloadPdfBuffer()`
- **Show Report** — Dropdown (`#showReportDropdown`) with four options: Takeoff Report for this Canvas Only, Takeoff Report for all Canvases on Page (hidden when single canvas), Takeoff Report for all Plan Pages and their Current Canvas, Takeoff Report for all Pages and Canvases; each opens report in new tab via `printReport(mode)`; `buildReportHtml(options)` supports `pageIndices` and `getAnnotations`; hidden when no counts or lines
- **Export PDFs** — Modal (`#specificPagesModal`): marker size and line width sliders (25–150%), Include takeoff report, Bundle highlights, Bundle notes toggles; page thumbnails with marked/unmarked/exclude; bulk actions All Marked Up / All Not Marked Up / Exclude All; Download exports selected pages; `downloadSpecificPages()`; uses `state.exportSettings.markerScale`, `state.exportSettings.lineScale`; hidden when no counts or lines
- **Copy to PipeTooling** — Dropdown (`#forPipeToolingDropdown`) below Export PDFs; yellow (primary) button; drop-up menu (always opens above); options: This Canvas Only (current page, active canvas; hidden when single page), All Visible Canvases (all pages, active canvas), All Canvases (all pages, merged); copies tab-delimited summary via `getPipeToolingSummary(options)`; counters as name/count/pages; line types as `[unit] of [name]`/length/pages; hidden when no counts or lines; sidebar links use https://pipetooling.com
- **Show Highlights / Show Notes** — Buttons open highlights or notes summary in new tab (not download); hidden when no highlights or no notes respectively; toggle switches in Export PDFs modal for bundling to PDF
- **Hotkeys** — M (Move), S (Set Scale), C (Counter), L (Line modal), J (Snap to horizontal/vertical), P (Polyline), D (Measure Distance — two-click; distance toast uses that Scale Zone’s scale when both clicks fall in the same zone, otherwise page scale), H (Highlight), X (Multiply Zone), N (Note), R (Rotate page); Shift+C = Quick tab when Counter modal open; Shift+L = Quick tab when Line modal open; arrow keys: Left/Right = page nav, Shift+Left/Right = previous/next marked page, Up/Down = switch canvas layers (when multiple canvases); Ctrl+Z / Ctrl+Shift+Z for Undo/Redo; Ctrl+R / Cmd+R (Refresh); ignored when focus is in input/textarea/contenteditable
- **Counter settings** — Ring section only visible when "Show ring around counters" toggle on; Show ring and Solid ring use toggle switches; Solid ring default is true; Show only counters on current page filters sidebar to counters with markers on the current page
- **Line type settings** — Show only line types on current page filters sidebar to line types with lines on the current page
- **Mobile header slot** — When no PDF: Upload PDF in header (transparent on mobile, text only); when PDF loaded: Set Scale (until scale set); `body.has-pdf` toggled in updateUI
- **Status bar** — Dual indicators: circle (dot) = canvas sync, square = PDF sync; each has a label next to it (e.g. "Canvas Synced with Cloud", "PDF Synced with Cloud", "PDF No PDF in project"); both use red/yellow/green/grey; messages: "Project not saved to cloud", "Upload PDF to start a project" (no PDF); Name / Upload / Save Project to Cloud / Load Project from Cloud; save progress shown during upload; **footer totals** (`#statusTotals` next to `#statusCoords`) display `[N | L unit]` (e.g. `[65 | 2,450 ft]`) — total counter count and total line length across every page and every canvas with multiply zones and scale zones applied; `computeFooterTotals` aggregates via `getMergedAnnotationsForPage` + `getMultiplyZoneForPoint` + `getLineLengthForTotals`; `getFooterTotalsCached` caches the result behind `footerTotalsDirty` (set by `invalidateFooterTotals()` from `markProjectDirty`) plus an automatic size-change check on `pageCount`/`counterCount`/`lineTypeCount` so project load/close/import/Prepare PDF commit/viewer-load recompute without touching their mutation sites; `.status-totals` CSS uses `font-variant-numeric: tabular-nums` so digits do not dance during fast cursor moves; hidden when `state.pages.length === 0`; unit picked by `pickScaleForLineType` (matches legend/sidebar); always shown including `[0 | 0 ft]` while a project is loaded
- **Pages badges** — `badge-scale-set` = yellow number when scale set; `badge-has-ann` = yellow outline when page has counts, lines, notes, highlights, multiply zones, or scale zones
- **Page rotation** — Per-page `page.rotation` (0/90/180/270); rotate button (↻) in zoom bar; `rotatePage90()` transforms annotations; notes text rotates with page; persisted in save/load
- **Counter/Line Type row** — Row click selects for placing; edit pen (✎) opens counterLineTypeDetailsModal (Name, Color, On pages, Delete)
- **Line drops** — Per-line `startDrop` and `endDrop` (in page scale units) for vertical runs; X markers at endpoints when drop > 0; included in total length via `getLineLengthPdfPts`; Line Properties modal (edit pen or context menu) shows source line type name, then Name, Color, Start drop, End drop, +1/+10/-10/-1/Clear buttons; sidebar Lines list shows "5 + 3 ft" below badge when drops; persisted in annotations
- **Canvas context menu** — `#contextMenu`; `handleContextMenu` → `hitTest` → `state.ctxTarget`; `showContextMenu` toggles actions by type (Edit note, Line Properties, Show Length, Assign to group, Edit multiplier/scale, Delete); below **Delete**, `#ctxTargetNameRow` (`.context-menu-target-name`) shows counter name for markers (`state.counters` / `typeId`) or line type name for quick lines and polylines (`state.lineTypes` / `lineTypeId`, same idea as `#linePropertiesLineType`); row hidden for other hit types; long-press on touch also opens menu; not available in view mode (`handleContextMenu` returns early)
- **Line types** — Have `curveStyle: 'straight' | 'arc'` (default `'straight'`); when `arc`, quick lines render as quadratic Bezier arcs and use arc length for totals; polylines remain straight; include `curveStyle` in save/load and export/import
- **Marked page navigation** — ‹‹ and ›› buttons outside page nav; jump to previous/next page with annotations; yellow text and border when selectable
- **Auto-save** — Every 5 seconds when dirty: signed-in to Supabase, unsigned to localStorage; 5-second localStorage backup for all users; `markProjectDirty()`, `performAutoSave()`, `autoSaveDirty`, `lastSaveIncludedPdf`, `savePdfInProgress`; **Save status** — in-modal bell `saveStatusBtn` and header bell `saveStatusBtnHeader` (`.header-save-status-btn`); both always visible when signed in (header hidden on mobile), default gray, `save-status-bell-attention` (yellow) shared by both on sync failure or checkout expiry; in-memory `saveStatusLog` (300s), `pushSaveEvent`, `lastCloudSaveAttemptFailed`, `checkoutExpiredNeedsAttention`; `saveStatusModal` live-ticks via `saveStatusModalTickTimer` + `renderSaveStatusModalContent` every 5s while open to refresh ago labels (cleared on close); sync/auth failures surface via Save Status bell (not generic toast); checkout/session expiry surfaces via Save Status **and** a one-shot toast (`CHECKOUT_EXPIRED_TOAST_MSG`, gated by `checkoutExpiredToastShown`); **Checkout keep-alive / lock probe** — `probeCheckoutLock(runId)` (server-side truth via `refresh_checkout_activity` RPC); `checkoutKeepalive` runs every `CHECKOUT_KEEPALIVE_MS` (10 min) when visible and checked out; `CHECKOUT_NEAR_EXPIRY_MS` (5 min) and `CHECKOUT_SOFT_GRACE_MS` (60 s) drive the 25-min probe boundary and the >31-min hard-skew shortcut in `performAutoSave` and the SaveProject modal; client no longer calls `check_in_project` voluntarily; on confirmed expiry `suspendAutoSaveUntilCheckout` halts the autosave loop until the user re-checks-out (cleared by `clearCheckoutExpiredAttention()` which also clears the toast flag); PDF IndexedDB cache on save; last-project restore from `clickcount-last-project` — on boot prompts Keep/Discard via `lastSessionRestoreModal` before restoring; Discard clears local state and deletes project from cloud if owner; save-before-load prompt when loading with unsaved changes; `withTimeout` wraps Supabase calls in performAutoSave and doTurnIn to prevent hang; `takeoff-state` includes `projectId` and `lastModifiedAt` when a project is loaded; last-project restore prefers takeoff-state backup when it is newer than Supabase; `autoSaveDirty` restored when performAutoSave fails so Turn in retries; `performAutoSave` acquires `saveInProgress` in try/finally so concurrent autosave + manual save serialize; `doTurnIn`, `saveBeforeLoadSave`, and `performSaveProjectToCloud` clear `autoSaveDirty` BEFORE awaiting and restore on failure so edits made during the save are not wiped; `performSaveProjectToCloud` on failure sets `lastCloudSaveAttemptFailed = true` and calls `updateSaveStatusIndicator()` so both bells turn yellow; `saveBeforeLoadSave` opens `openLoadProjectModal()` on success when no `pendingCopyProject` (regular Load flow); PDF upload (`pdfInput`), Prepare PDF (`commitPreparePdfToState` callers), and Import Canvas (`importInput`) call `markProjectDirty` so cloud reflects them; `performSaveProjectToCloud` recovers a detached `state.pdfBuffer` via `pdfCacheGet(currentProjectId, pdfHash)` and logs `pushSaveEvent('manual_save_recover', …)`, otherwise (no cache and no `pdfStoragePath`) returns a clear error instead of silently skipping the upload; `withTimeout` wraps every Supabase call in `performSaveProjectToCloud` (10 s `pdf_hash` select / `storage.info`, 5 s `pdfCachePut`, 30–60 s inserts/updates/uploads); local `saveWithTimeout` helper removed; `takeoffBackupPut` reads the existing IDB record first and skips/aborts when the incoming `lastModifiedAt` is older (`saveDebugLog('takeoffBackup.skip_stale', …)`); custom-icon upload (`customIconUploadInput.onchange`) calls `markProjectDirty` after `saveUserCustomIcons` so the next autosave picks up new icons; PDF merge (`pdfInput.onchange`) re-checks the 50 MB ceiling by summing `buffersForMerge` byte sizes before `mergePdfBuffers` and reverts `state.pages` to `startPageIdx` + prunes `activeCanvasIdByPage` + alerts when the projected size exceeds `PDF_MAX_SIZE_BYTES`; manual-save `pdfCachePut` failure surfaces a one-shot `pushSaveEvent('manual_save_cache_warn', …)` gated by `pdfCacheWarnShown` (reset on sign-out) so a detached-buffer recovery risk is visible in the Save Status modal without log spam; force-turn-in flush in `refreshProjectPermissions` classifies the failed `performAutoSave` result into `force_turn_in_flush_blocked` (lock taken / RLS / permission) vs `force_turn_in_flush_err` and keeps `autoSaveDirty = true` so the next checkout retries; `isTransientSaveError` plus a single 500 ms retry loop inside the `saveInProgress` mutex covers both `performAutoSave` and `performSaveProjectToCloud` (latter only when `originalProjectId` was set at entry, to avoid duplicate-insert hazard on transient errors during INSERT), emitting `autosave_retry` / `manual_save_retry`; `takeoffBackupPut` emits a one-shot `pushSaveEvent('takeoff_backup_warn', …)` gated by `takeoffBackupWarnShown` (reset on sign-out) when the IDB transaction throws so tab-crash-recovery breakage is visible; SaveProject modal blocks at a confirm when `!includePdf && state.pdfHash !== projects.pdf_hash` (`manual_save_canceled` on cancel, `manual_save_pdf_mismatch_accepted` on accept), preventing the foot-gun where a user saves a new PDF version's annotations against the old cloud PDF; `performSaveProjectToCloud` captures `prevPdfStoragePath` at entry and best-effort `storage.from('pdfs').remove([prevPdfStoragePath])` (10 s `withTimeout`) after a successful PDF replacement, logging `manual.save.pdf_cleanup_ok` or `_err`; `subscribeToProjectCheckoutChanges` is async with `projectsCheckoutGeneration` token gating every callback so rapid project switches do not leak channels; `updateSaveStatusIndicator` adds `save-status-bell-offline` (dim gray) class and offline-specific title when `navigator.onLine === false`; migration 039 sets `projects.updated_at` server-side on every UPDATE so multi-tab staleness comparisons against IDB backups are skew-resistant; `doTurnIn` is reentrancy-guarded by a `turnInInProgress` module-level flag, awaits `writeTakeoffStateBackup` so the local recovery point is durable before the cloud RPC, returns `'Sync in progress, try again in a moment'` with `turn_in_save_in_progress` event when `autoSaveDirty && saveInProgress` at entry, pushes `turn_in_blocked_by_save_err` (in addition to inner `autosave_err`) when the pre-checkin save fails, retries `check_in_project` once (500 ms backoff) on transient errors using `isTransientSaveError` (`turn_in_retry` event); a local `progress(stage, label)` helper mirrors every `setTurnInProgress` UI label into a `turn_in_stage` event carrying `{stage, elapsedMs}` (stages: `pre_probe`, `local_backup`, `sync_to_cloud`, `release_lock`, `retry`); one `turn_in_start` event at entry captures `{onLine, network: captureNetworkInfoDetail(), lastOk: lastSuccessfulSupabaseCallAt, failures, dirty, saveInProgress, projectId}`; `turn_in_ok` carries `{elapsedMs, attempts}`; `turn_in_err` and `turn_in_already_released` enrich the detail JSON with `{elapsedMs, stage, attempt, online, network}` so the Export logs envelope shows exactly which stage burned the timeout budget; and `doTurnInAndHandleResult` detects "you do not have this project checked out" / `NOT_CHECKED_OUT` / `not_owned` in `result.error` to surface `turn_in_already_released` + "You no longer hold the checkout - refreshing." toast + `refreshProjectPermissions` instead of a generic failure; migration 040 extends `check_in_project` and `force_check_in_project` to return `server_now` and the client calls `updateServerClockFromRpc(data)` at every check-in (`doTurnIn`, `checkInCurrentProjectIfHeld` sign-out helper, `settingsForceCheckIn`, Manage Projects admin force turn-in); `subscribeToProjectCheckoutChanges` uses `.subscribe((status, err) => …)` with capped backoff reconnect (`projectsCheckoutReconnectTimer`, `PROJECTS_CHECKOUT_RECONNECT_BACKOFF_MS` = 1s/3s/10s/30s) and forces one `refreshProjectPermissions` on `SUBSCRIBED`; force-turn-in path in `refreshProjectPermissions` fires a best-effort flush before flipping to viewer, logs `pushSaveEvent('force_turn_in', …)`, and the toast warns when there were unsaved edits; checkout RPCs (`check_out_project`, `refresh_checkout_activity`) return `server_now` and `checked_out_at` (migration 038), client tracks `serverClockOffsetMs` via `updateServerClockFromRpc` and uses `serverNowMs()` for all expiry math so clock skew on the user's machine never produces false "edit session expired" toasts; `writeTakeoffStateBackup()` called before Turn in; backup fetches `proj.pdf_hash` from Supabase when `last.pdfHash` is null, so IndexedDB backup can populate PDF from cache for last-project scenarios; `writeTakeoffStateBackup` is async; runs on visibilitychange (hidden) to capture state before tab close; debounced 1s after `markProjectDirty`; 5s interval; `commitPreparePdfToState` passes `trimmedBuf.slice(0)` to `pdfjsLib.getDocument()` so buffer stays intact for save/backup; **Sync hardening** (PR 1) — `withTimeout(promiseOrFactory, ms, label)` accepts a `(signal) => promise` factory and exposes `.controller` on the returned promise; the 7 Postgrest write call sites in `performAutoSave` and `performSaveProjectToCloud` use the factory form with `.abortSignal(signal)` so the underlying fetch is aborted when the timer fires (frees dead H2 sockets); autosave update timeout dropped to `AUTOSAVE_TIMEOUT_MS` (15 s) while manual-save timeouts remain 30 s / 60 s; `performAutoSave` publishes `inFlightAutoSaveController` per attempt (nulled in `finally`); `noteAutoSaveOutcome(ok, err)` tracks `consecutiveAutoSaveFailures`, `firstAutoSaveFailureAt`, sets `nextAutoSaveAttemptAt = now + AUTOSAVE_BACKOFF_LEVELS_MS[Math.min(N-1, 3)]` (5/15/30/60 s capped), shows `#syncPausedBanner` at `AUTOSAVE_BANNER_THRESHOLD` (3) failures, emits `autosave_recovered` on first success after failures; autosave interval gates on `Date.now() < nextAutoSaveAttemptAt` and logs `autosave.skip / reason: backoff` when debug enabled; `#syncPausedBanner` (above status bar) "Cloud sync paused - your work is saved locally. Reconnecting..." with `#syncPausedBannerRetry` button calling `retrySyncNow()` (aborts in-flight, resets backoff, `autoSaveDirty = true`, refreshes session via `supabase.auth.getSession()`, emits `manual_sync_retry`); `visibilitychange -> hidden` aborts `inFlightAutoSaveController` unconditionally before the fire-and-forget pre-close save so the fresh save opens a new socket; both abort sites set `autoSaveAbortReason` (`'user_retry'` / `'hidden'`) before calling `controller.abort()` and the `performAutoSave` catch detects the flag and returns early (no `consecutiveAutoSaveFailures` bump, no backoff, no `autosave_err` event, no yellow bell) so deliberate cancellations do not pollute the failure counter; **Sync hardening (PR 2)** — `runRecoveryProbe(trigger)` fires fire-and-forget at the 5th consecutive autosave failure (`AUTOSAVE_RECOVERY_THRESHOLD`) and on every `window.online` event when `consecutiveAutoSaveFailures > 0`; refreshes JWT via `supabase.auth.getSession()` then raw `fetch(SUPABASE_URL + '/rest/v1/projects?select=id&limit=1', { cache: 'no-store', signal, headers: { apikey, Authorization } })` with `AUTOSAVE_RECOVERY_TIMEOUT_MS = 5000` AbortController timeout (bypasses Supabase client pool so dead H/2 sockets get replaced); on success only `nextAutoSaveAttemptAt = 0` (real save success still drives `consecutiveAutoSaveFailures` reset via `noteAutoSaveOutcome(true)`, which also clears `recoveryProbeFiredForFailureCount` and `autosaveMilestoneFiredAt`); `recoveryProbeInFlight` re-entrancy flag; `recoveryProbeFiredForFailureCount` per-failure-run dedupe; emits `autosave_recovery_probe` / `autosave_recovery_ok` / `autosave_recovery_err`; milestone events `autosave_failing_3` / `autosave_failing_5` / `autosave_failing_10` fire once per failure run via `autosaveMilestoneFiredAt` dedupe; `captureNetworkInfoDetail()` attaches `{ effectiveType, downlink, rtt, saveData }` (Chromium only; silently skipped elsewhere); P8 `recordAutosaveLatency(updMs)` runs after each `projects.update` perfLog, pushes into `autoSaveLatencySamples` (capped at `AUTOSAVE_SLOW_WINDOW = 20` via `shift()` once full), emits `autosave_slow` `{ p95, n, latest }` when `p95 > AUTOSAVE_SLOW_MS = 1000` ms after `AUTOSAVE_SLOW_MIN_SAMPLES = 10` samples, debounced by `autosaveSlowEmittedAt` + `AUTOSAVE_SLOW_DEBOUNCE_MS = 60000`; inserts and storage.info calls are not sampled (too rare to influence p95); **Sync hardening (PR 3)** — every autosave/manual-save/turn-in event funnels through `autosaveEventDetail(extra)` so the exported envelope is self-sufficient (always-on `failures`, `online`, `msSinceLastSuccess`, `network` from `captureNetworkInfoObj()`, plus per-event `runId`/`elapsedMs`/`attempt`/`phase`/`usedRawFetch`/`opMs`/`storageInfoStatus`/`storageInfoMs`/`code`/`status`/`name`); previously verbose-only `saveDebugLog('autosave.storage.info.error', …)` and `autosave.request.start`/`.ok` now emit always-on `pushSaveEvent` siblings `autosave_storage_info_err`/`autosave_storage_info_skipped`/`autosave_request_start`/`autosave_request_end` (every supabase-js or raw-fetch projects.update/insert wraps in matching start/end); `dirty` events carry `dirtyForMs`; on `markProjectDirty` first transition `dirtyStartedAt` stamps so the 5s autosave interval triggers `writeSaveLogsSnapshot('dirty_10min')` once when `Date.now() - dirtyStartedAt >= DIRTY_SNAPSHOT_THRESHOLD_MS` (10 min) gated by `envelopeSnapshotDirtyStamp` (reset on `autosave_ok`); IDB version bumped to 5 with new `save_logs_snapshots` store (`SAVE_LOGS_SNAPSHOT_STORE`, capped at `SAVE_LOGS_SNAPSHOT_MAX_ENTRIES = 10`) holding `{capturedAt, envelope}` rows written by `writeSaveLogsSnapshot(reason)` (also fires on `autosave_failing_5` milestone, throttled by `envelopeSnapshotFiredAt` 60s) and read by `readSaveLogsSnapshots(limit=5)`; `buildSaveLogsEnvelopeWithSnapshots()` is the async wrapper used by the Save Status modal Export logs / Copy logs buttons and attaches `envelope.autoSnapshotEnvelopes = readSaveLogsSnapshots(5)` plus `envelope.autoSnapshotReason` on each snapshot, so a tab can be reloaded between an incident and the user opening the modal without losing the failure timeline; `runRecoveryProbe` chains into `runRecoveryProbeAndMaybeRecycle(trigger)` which on raw-probe OK runs `runSupabaseClientProbe(trigger)` (`supabase.from('projects').select('id').limit(1)` with `CLIENT_PROBE_TIMEOUT_MS = 5000` and `clientProbeInFlightGuard` re-entrancy; logs `autosave_client_probe_ok`/`_err`) and on client-probe failure or throw calls `recreateSupabaseClient(reason)` which `await supabase.removeAllChannels()` (nulls `projectsCheckoutChannel`), calls `createClient(SUPABASE_URL, SUPABASE_ANON_KEY)` again, reattaches via `supabase.auth.setSession({access_token, refresh_token})` (5s `withTimeout`), and re-runs `subscribeToProjectCheckoutChanges(state.currentProjectId)` to restore the realtime channel — gated by `clientRecycleInFlight` and `CLIENT_RECYCLE_COOLDOWN_MS = 30000` from `lastClientRecycleAt`, counts via `clientRecycleCountThisRun` (reset on `autosave_ok`), and emits `autosave_client_recycled`/`autosave_client_recycle_err`; `STORAGE_INFO_TIMEOUT_MS = 3000` (down from 10s) and storage.info is skipped entirely while `consecutiveAutoSaveFailures > 0` or after a per-call `storageInfoFailedThisCall` flag (emits `autosave_storage_info_skipped` with `reason: 'skipped_degraded' | 'skipped_failed'`), reclaiming the budget for the actual update; raw-fetch fallback for `projects.update`: `rawProjectsUpdate(projectId, payload, signal)` PATCHes `SUPABASE_URL + '/rest/v1/projects?id=eq.X'` with `apikey + Authorization: Bearer + Content-Type: application/json + Prefer: return=minimal`, `cache: 'no-store'`; engages when `consecutiveAutoSaveFailures >= 3` OR (within one `performAutoSave` run) the previous attempt was a supabase-js `projects.update` that timed out; both branches still wrap in the same `withTimeout(..., AUTOSAVE_TIMEOUT_MS)` so the `inFlightAutoSaveController` and `autoSaveAbortReason` paths are unchanged; same gating applied to `performSaveProjectToCloud`'s no-PDF update; emits `autosave_via_raw_fetch_ok`/`_err` (and `manual_save_via_raw_fetch_ok`/`_err`); raw-fetch fallback for Turn In: `rawCheckInProject(projectId, signal)` POSTs `SUPABASE_URL + '/rest/v1/rpc/check_in_project'` with the same headers and `{p_project_id}` body; `doTurnIn` enables it when `consecutiveAutoSaveFailures >= 3` or as the second attempt after the supabase-js RPC failed once; preserves `updateServerClockFromRpc(data)` and the `alreadyReleased` regex; emits `turn_in_via_raw_fetch_ok`/`_err`; `doTurnIn` also tracks `stageStartedAt` so each `progress(stage, label)` transition emits a matching `turn_in_phase_done` carrying `{stage, durationMs, elapsedMs}`, and the final stage closes its phase before `turn_in_ok` or `turn_in_err`; `turn_in_ok` and `turn_in_err` now also carry `usedRawFetchForCheckIn`; **Checkout expired recovery UX** — when a Turn In click returns `CHECKOUT_EXPIRED`, instead of the one-shot `CHECKOUT_EXPIRED_TOAST_MSG`, the client opens `#checkoutExpiredRecoveryModal` (z-index 220 so it stacks over Project Settings; helpers `openCheckoutExpiredRecoveryModal({trigger})` and `closeCheckoutExpiredRecoveryModal()`; sub-modes via `applyCheckoutExpiredRecoveryMode('default' | 'someone_else' | 'error', ctx)`): primary `Re-check out and save` -> `reCheckOutAfterExpiry(trigger)` which calls `supabase.rpc('check_out_project')` (8 s `withTimeout`, `CHECK_IN_TIMEOUT_MS`), on success runs `clearCheckoutExpiredAttention()` + `state.checkedOutBy = self` + `state.canCheckOut = false` + `state.isViewer = false` + fire-and-forget `performAutoSave('checkout_recovered')` so the dirty edits flush, on rpc failure `refreshProjectPermissions()` then returns `{ok:false, otherEmail}` (modal swaps to "Someone else is editing" copy + hides re-check button) or `{ok:false, error}` (modal switches to error mode with retry-able primary button); secondary `Export local backup` re-clicks `#exportBtn` so the JSON drops without leaving the modal; tertiary `Discard local edits and reload` requires a `confirm()`, gates on `!saveInProgress && !turnInInProgress`, clears `autoSaveDirty`, calls `takeoffBackupDelete(state.currentProjectId)` to wipe the IDB recovery point so the next boot does not restore the discarded state, emits `checkout_recover_discarded`, and `location.reload()`; `doTurnInAndHandleResult` short-circuits to the modal at entry when `checkoutExpiredNeedsAttention` is already set (so subsequent `[Turn In]` clicks open the modal without burning a 25 s `pre_probe + local_backup + sync_to_cloud` cycle), logging `turn_in_short_circuit_expired`; one-shot `checkoutExpiredToastShown` gate stays for the background discoveries (`performAutoSave`, `checkoutKeepalive`, `probeCheckoutLock`) but the Turn In CHECKOUT_EXPIRED branch no longer guards the user-visible callout behind it; the header `headerEditStatusBanner` and `sidebarCheckoutBanner` add a fourth state `edit-status-expired` with `header-edit-status-btn-expired` (yellow, pulsing) labeled `[Edit session expired — Re-check out]` and `dataset.action = 'checkout_expired_recover'`, branched ahead of `[Turn In]` in `updateUI` so the user always sees a recovery affordance while `checkoutExpiredNeedsAttention === true`; `handleEditStatusBannerClick` routes the new action to `openCheckoutExpiredRecoveryModal({trigger:'expired_banner'})`; the Save Status modal renders `#saveStatusExpiredCallout` (yellow callout above the canvas/PDF summary) with `Re-check out and save` and `Export local backup` buttons whenever `checkoutExpiredNeedsAttention === true` (toggled by `renderSaveStatusModalContent` on every 5 s tick so the live modal updates); save-status callout primary button opens the recovery modal as a fallback on failure; events: `checkout_recovered {trigger, msSinceExpiry, elapsedMs, dirty}`, `checkout_recover_blocked {trigger, otherEmail, elapsedMs}`, `checkout_recover_err {trigger, message, status, elapsedMs}`, `checkout_recover_discarded {projectId}`, `turn_in_short_circuit_expired`; expiry-age label inside the modal uses `computeCheckoutExpiryAgeMs()` over `state.checkedOutAt + CHECKOUT_INACTIVITY_MS`, the most recent `checkout_expired`/`keepalive_expired` event from `saveStatusLog`, and `lastSuccessfulSupabaseCallAt + CHECKOUT_INACTIVITY_MS` (smallest wins); `checkoutExpiredRecoveryInFlight` module flag dedupes concurrent presses; **Sync hardening (PR 5)** — silent self-heal for the two most common Wendi-style failures, layered under the recovery modal so the modal is now a fallback rather than the default; (1) `handleBackgroundCheckoutExpired(trigger)` is a single wrapper used by all background `CHECKOUT_EXPIRED` detection sites (autosave loop catch at the bottom of `performAutoSave` consumer, `visibilitychange -> visible` post-probe, `checkoutKeepalive` 10-min probe — the explicit user paths `doTurnInAndHandleResult`, `saveProjectDo`, and `saveBeforeLoadSave` still open the modal directly because the user explicitly clicked); it sets `checkoutExpiredNeedsAttention = true` + `suspendAutoSaveUntilCheckout = true`, emits `checkout_expired` with `{trigger}`, calls `tryAutoRecheckoutIfAllowed(trigger)`, and only falls back to the one-shot `CHECKOUT_EXPIRED_TOAST_MSG` (gated by `checkoutExpiredToastShown`) when auto-recovery is blocked; (2) `tryAutoRecheckoutIfAllowed(trigger)` runs gated by `AUTO_RECHECKOUT_MAX_PER_PROJECT = 3` per `autoRecheckoutCountByProject` Map entry, `AUTO_RECHECKOUT_MIN_GAP_MS = 5000` against `lastAutoRecheckoutAt`, and `state.canCheckOut === true` after a fresh `refreshProjectPermissions()` (someone else holding the lock or not-allowed both fall back to the modal); emits `auto_recheckout_attempt`, `auto_recheckout_ok`, `auto_recheckout_blocked {reason: 'no_project' | 'viewer' | 'min_gap' | 'cap_reached' | 'not_allowed'}`, or `auto_recheckout_err`; (3) `reCheckOutAfterExpiry(trigger, opts)` now takes `{silent: true}` so the auto path suppresses the user-visible "Re-checked out..." toast (the project still resumes autosave via the existing fire-and-forget `performAutoSave('checkout_recovered')`); (4) `resetAutoRecheckoutCounter(projectId)` (no arg = clear whole Map) is called on auth sign-out (Map.clear), on `settingsCloseProject` (per-project), and on explicit user check-out (header banner, settings checkout, recovery modal "Re-check out and save") so the user's explicit action gives them a fresh cap; (5) `projects.insert` always uses raw fetch — new helper `rawProjectsInsert(payload, signal)` mirroring `rawProjectsUpdate` (POST `/rest/v1/projects` with `Prefer: return=representation`, `cache: 'no-store'`, returns `{data, error}` so the call sites destructure unchanged); all three insert call sites (`performSaveProjectToCloud` with-PDF and without-PDF, `performAutoSave` new-project create) bypass the supabase-js client unconditionally and emit `manual_save_via_raw_fetch_ok/_err` or `autosave_via_raw_fetch_ok/_err` plus the existing `autosave_request_start/_end` (with `raw: true`); `lastAttemptUsedRawFetch = true` set at the autosave insert site so the surrounding telemetry reflects the path; every project teardown / switch site (sign-out, Close Project, Load Project from Cloud no-PDF + with-PDF, delete current project, `applyLocalForkAfterPdfLoad`, PDF-mismatch decline, last-session backup / view-link restore, boot restore in `loadProjectFromCloud`, and the three new-project insert sites in `performSaveProjectToCloud` + `performAutoSave`) now calls `clearCheckoutExpiredAttention()` so `checkoutExpiredNeedsAttention` / `suspendAutoSaveUntilCheckout` / `checkoutExpiredToastShown` cannot leak from project A into project B in the same tab; `performAutoSave` adds a suspend gate at entry — when `suspendAutoSaveUntilCheckout === true` and `externalRunId !== 'checkout_recovered'`, it returns `{ok:false, error:{code:'CHECKOUT_EXPIRED'}}` and emits `saveDebugLog('autosave.skip', { reason: 'suspended_pending_recheckout' })` so direct callers (`doTurnIn` pre-checkin save, `saveBeforeLoadSave`, `refreshProjectPermissions` force-turn-in flush, `visibilitychange -> hidden` flush) get the same short-circuit the 5s interval has; the autosave new-project insert path (`rawProjectsInsert` success in `performAutoSave`) now mirrors the two manual-save insert sites by calling `subscribeToProjectCheckoutChanges(projectId)` and hydrating `state.projectOwnerId = user.id` + `state.loadedViaViewLink = false` + `state.isViewer = false` + `state.canCheckOut = true` + `state.checkedOutBy/At/Email = null`, so autosave-created projects receive realtime force-turn-in notifications and expose the gear-menu Check out button to their owner; `saveBeforeLoadSave`'s CHECKOUT_EXPIRED branch now mirrors `doTurnInAndHandleResult` — sets both `checkoutExpiredNeedsAttention = true` and `suspendAutoSaveUntilCheckout = true`, calls `refreshProjectPermissions()`, hides `saveBeforeLoadModal`, clears `pendingCopyProject`, and opens `openCheckoutExpiredRecoveryModal({ trigger: 'save_before_load' })` so users who hit the expiry from a Load / Copy flow get the same recovery affordance as the Turn In path instead of being stranded on the "save before load" prompt; **Sync hardening (PR 6)** — sign-out teardown foundation. New `resetLocalSessionState({keepArtboard})` helper clears pages, project ids, PDF buffers, dirty/save/turnIn flags, undo stacks, pending canvas/copy state, checkout fields, plus calls `resetAutosaveDegradedState()` (which resets `consecutiveAutoSaveFailures`, `firstAutoSaveFailureAt`, `nextAutoSaveAttemptAt`, `recoveryProbeFiredForFailureCount`, `autosaveMilestoneFiredAt`, `autoSaveLatencySamples`, `autosaveSlowEmittedAt`, `envelopeSnapshotFiredAt`, `envelopeSnapshotDirtyStamp`, `dirtyStartedAt`, `clientRecycleCountThisRun`, `lastClientRecycleAt`, `lastClientProbeAt/Ok/Ms`, `autoSaveAbortReason`, hides `#syncPausedBanner`) and clears `saveStatusLog`, `state.userActivityAllRowsCache`, `state.userActivityViewMode`, `autoRecheckoutCountByProject`, `lastAutoRecheckoutAt`, `lastCheckoutRefreshAt`, `pdfCacheWarnShown`, `takeoffBackupWarnShown`, plus `clearCheckoutExpiredAttention()`; `SIGNED_OUT` branch in `onAuthStateChange` now calls `resetLocalSessionState()` (default `keepArtboard:false` so counters/lineTypes also clear) and `settingsCloseProject` calls `resetLocalSessionState({keepArtboard:true})`; `lastAuthUserId` module-level tracks the signed-in user id, and `TOKEN_REFRESHED` runs full `resetLocalSessionState()` + presence/admin re-hydration when `session.user.id !== lastAuthUserId` (emits `auth_user_changed_on_refresh`); `BroadcastChannel('clickcount-auth')` posts `{kind:'signed_out'}` on sign-out and other tabs receive via the channel or the `clickcount-signout-broadcast` localStorage fallback, running `handleCrossTabSignOut` which calls `resetLocalSessionState()`, nulls `state.supabaseSession`, and emits `cross_tab_signout`; `checkInCurrentProjectIfHeld` now wraps `check_in_project` in `withTimeout(..., CHECK_IN_TIMEOUT_MS, 'Sign-out check-in')` and swallows timeouts via `pushSaveEvent('signout_checkin_timeout', …)` so a stuck RPC never blocks `signOut()`; **Sync hardening (PR 8)** — dirty-flag correctness. New `dirtyGeneration` counter incremented by every `markProjectDirty()`. `performAutoSave` and `performSaveProjectToCloud` both capture `genAtEntry = dirtyGeneration` at the top, clear `autoSaveDirty = false` inside the save function, and on success set `autoSaveDirty = (dirtyGeneration !== genAtEntry)` so user edits typed during a save are not silently lost. On failure `autoSaveDirty = true` is restored (manual save also OR-s with `wasDirty`). The autosave interval, `doTurnIn`, and `saveBeforeLoadSave` no longer pre-clear `autoSaveDirty`; the save functions own that lifecycle. `reCheckOutAfterExpiry` awaits the recovery `performAutoSave('checkout_recovered')` via a module-level `inFlightRecoverySavePromise`, and `doTurnIn` `Promise.race`-waits up to 8s on that promise before starting so a Turn In click during recovery does not skip the save. `refreshProjectPermissions` retries `list_accessible_projects` once with 500ms backoff on RPC error and emits `refresh_permissions_err` instead of silently corrupting checkout state. Force-turn-in flush now skips `performAutoSave()` when `suspendAutoSaveUntilCheckout === true` and emits `force_turn_in_flush_skipped_suspended`; **Sync hardening (PR 7)** — cross-user data hygiene. `takeoffBackupGet(projectId, currentUserId)` now checks `entry.userId` and on mismatch logs `takeoffBackup.user_mismatch`, calls `takeoffBackupDelete(projectId)`, and returns null (callers in `doRestoreLastProject`, Copy-to-new modal, Load Project modal, and last-session restore all pass `state.supabaseSession?.user?.id`). Custom icons IndexedDB key is per-user: `customIconsCurrentKey()` returns `customIcons_${userId}` when signed in (falls back to legacy `'user'` only when no session); on first read for a signed-in user with no per-user entry, the legacy `'user'` entry migrates to the per-user key (emits `customIcons.migrated_to_per_user`); `resetLocalSessionState({keepArtboard:false})` clears `customIconsCache`, and the `SIGNED_IN` branch re-loads from IndexedDB when cache is empty. `refreshProjectPermissions` when `!proj` after a successful RPC now flips to viewer + suspends autosave + toasts "You no longer have access to this project." instead of silently returning (emits `permissions_project_missing`). Boot-time last-session restore (`init()` IIFE) tags a `PGRST116`/denied/permission error as `projectAccessDenied` and wipes both `localStorage.clickcount-last-project` and the IDB takeoff backup, emitting `last_session_restore_skip_inaccessible`; **Sync hardening (PR 10)** — save-path failure safety. `performSaveProjectToCloud` with-PDF new-project flow no longer hydrates `state.currentProjectId` / `state.projectOwnerId` / checkout fields immediately after the project insert; instead it captures `orphanProjectIdForCleanup = row.id` + `pendingNewProjectHydration = {projectId, userId}` and defers state hydration until after the PDF upload AND `projects.update` complete (just before `break manualSaveLoop`). On failure between insert and update, the catch block deletes the orphan row via `supabase.from('projects').delete().eq('id', orphanId)` with a 5s `withTimeout`, emitting `manual_save_orphan_cleanup_ok` or `_err`. Centralized `assertPdfWithinLimit(bytes, context)` checks the 50MB ceiling and emits `pdf_size_exceeded`; called from `commitPreparePdfToState` (before mutating `state.pages` / `state.pdfBuffer`) and from `performSaveProjectToCloud` before `storage.upload`. `writeTakeoffBackupToIndexedDB` is gated by a module-level `takeoffBackupWriteInFlight` promise — concurrent calls (5s interval, `visibilitychange`, debounced `markProjectDirty`, manual Turn In, failed saves) reuse the in-flight write rather than open overlapping IDB transactions, emitting `takeoff_backup_skip_inflight`. `writeTakeoffStateBackup` awaits the in-flight backup when one exists rather than starting a second one; **Sync hardening (PR 11)** — boot + view-link order. Boot order now `initSupabaseAuth()` BEFORE the `takeoffBackupGet('local', uid)` + `applyTakeoffBackupToState` step, so cross-user takeoff data never flashes on the canvas before auth resolves; legacy `localStorage.takeoff-state` parsed at boot is also filtered (when `parsed.userId && parsed.userId !== currentUid` the entry is removed and `takeoff_backup_skip_other_user` event is emitted). `initViewOnlyMode` now follows up the view-only setup with `initSupabaseAuth()` so view-link tabs see the current Supabase session (forces `state.isViewer = true`, `state.canCheckOut = false`, `state.loadedViaViewLink = true`; emits `view_link_session_attached`). `doGlobalReloadNow` writes a `clickcount-pending-global-reload` stamp instead of `clickcount-last-global-reload` directly; the actual `GLOBAL_RELOAD_STAMP_KEY` commits only after `load`/`pageshow` confirms the reload happened, emitting `global_reload_committed`. If the reload is blocked (extension intercept, navigation cancel), the pending stamp stays and `checkGlobalForceReload` will retry on the next opportunity instead of marking it done prematurely; **Sync hardening (PR 9)** — manual-save expiry parity. Manual Save's `confirmedExpired` branch (Save Project modal) now stops flipping `state.isViewer`/`canCheckOut` locally and instead calls `await handleBackgroundCheckoutExpired('manual_save')` + `refreshProjectPermissions()`; if `silentlyRecovered:false`, it hides the Save Project modal and opens `openCheckoutExpiredRecoveryModal({trigger:'manual_save'})` so the Manual Save expiry UX matches Turn In and `saveBeforeLoadSave`. `handleBackgroundCheckoutExpired` is now reentrancy-guarded by `backgroundCheckoutExpiredInFlight` (debug `checkoutExpired.skip_inflight`) so concurrent expiry detections (visibility + keepalive + autosave + manual save) produce one `checkout_expired` event and one recovery modal open. `markProjectDirty`'s `refresh_checkout_activity` call is gated on `!suspendAutoSaveUntilCheckout && !checkoutExpiredNeedsAttention` so edits in an expired state never accidentally extend the server lock. `tryAutoRecheckoutIfAllowed` no longer increments `autoRecheckoutCountByProject` before the RPC; on transient errors (`isTransientSaveError`) the cap is not consumed, only successes and definite failures count toward the per-project max. Admin force turn-in success handlers (`settingsForceCheckIn` and Manage Projects `forceCheckInProjectFromManage`) call `clearCheckoutExpiredAttention()` and `resetAutoRecheckoutCounter` for the affected project so the prior owner's UI is fully reset
- **Undo/Redo** — Last 5 moves in local memory; `undoStack`, `redoStack`, `pushUndoSnapshot()`, `clearUndoStacks()`; Undo/Redo buttons in bottom bar next to rotate; Ctrl+Z / Ctrl+Shift+Z; cleared on load, project switch, or when viewer
- **Middle mouse pan** — Hold middle mouse button to pan regardless of active tool; `state.isPanning`, `state.panStart`; move SVG cursor during pan; window mouseup releases when released outside canvas
- **Sharing** — `project_shares` table; checkout/turn-in (one editor at a time); 30-minute inactivity expiry (lock extends on edits/saves via `refresh_checkout_activity`, plus an idle keep-alive `checkoutKeepalive` every `CHECKOUT_KEEPALIVE_MS` while the tab is visible and the user holds the lock; `visibilitychange` returning to visible runs `probeCheckoutLock` before `refreshProjectPermissions`; `visibilitychange` to hidden fires a best-effort `performAutoSave` flush when dirty); system admin can force turn-in and check out any project (migration 029); `state.isViewer`, `state.canCheckOut`, `state.checkedOutBy`, `state.checkedOutEmail`; Load Project uses `list_accessible_projects` RPC (returns `owner_email`, `my_access_role` for filters — migration 036); Share modal adds users via `invite-to-project` Edge Function; when admin force turns in, user with project open gets realtime notification ("Project was turned in. You can check out to edit again.") and UI switches to view mode via `refreshProjectPermissions`; Manage Projects modal has Force turn-in (admin) per checked-out row; `list_projects_for_admin` returns `checked_out_by`, `checked_out_at`, `checked_out_email` (migration 025)
- **View links** — `project_view_links`, `view_link_access_log`; Share modal "View links" section: create, list, copy URL, access log, revoke; URL `?t=TOKEN`; `get-view-project` Edge Function (no JWT); email domain gate (clickplumbing.com); `viewCacheGet`/`viewCachePut` for IndexedDB; `viewLinkEmailModal`; `initViewOnlyMode(viewToken)` on boot when `?t=` present; Copy view link button (`#copyViewLinkBtn`) left of gear copies most recent view link (creates if none); create restricted to owners/editors — copyViewLinkBtn and shareViewLinkCreate hidden when `state.isViewer`
- **Canvas browsing in View mode** — Viewers can switch active canvas via pills and layers dropdown; change is local (no markProjectDirty); Add canvas and Edit canvas remain restricted
- **Toggle switches** — `.toggle-switch` with `.toggle-switch-knob`; button toggles hidden checkbox, syncs `aria-pressed`, dispatches change; used for Show group colors, Counter Settings (Show ring, Solid ring), Save Project Include PDF, Export PDFs (Bundle highlights, Bundle notes, Include report)
- **Counter button dynamic icon** — When counter tool selected and a counter chosen in sidebar, `counterBtn` and `counterBtnSidebar` show that counter's icon and color (with black outline) instead of default ellipse; `COUNTER_BTN_DEFAULT_SVG`; `headerActiveCounter` cleared
- **Custom icon upload** — Create Counter and Counter Details modals: Custom Icons grid has "+ Upload" cell; hidden `#customIconUploadInput` (accept .svg); `parseUploadedSvg`, `getUserCustomIcons`, `saveUserCustomIcons`, `getEffectiveCustomIcons`; user icons in IndexedDB `customIconPaths` (in-memory cache); included in export/import; click "Custom Icons" label to open customIconTipsModal with upload tips; Manage Icons modal Custom icons section: Edit toggles edit mode, select rows, Delete selected removes from IndexedDB
- **Bundled custom icons** — SVGs in `my-counters/` → run `npm run build:icons` → paste output into `CUSTOM_ICONS` in index.html; see CUSTOM_ICONS.md
- **Sidebar collapse** — Clicking the collapse icon (▼/▶) or the black space next to it minimizes the section; `.collapse-icon` in sidebar has expanded padding for larger hit area; Groups and Lines sections start minimized by default
- **Multiply Zone tool** — Draw rectangular zone; apply multiplier to counts and line lengths for items whose start/end fall inside; `ann.multiplyZones`; first containing zone wins when overlapping; two-click flow; multiplyZoneModal (preview, multiplier input); context menu Edit multiplier, Delete; right-click multiply zone icon (header/sidebar) opens multiplyZoneSettingsModal; openMultiplyZoneSettingsModal; X hotkey; hidden for viewers
- **Scale Zone tool** — Two-click rectangle (like Multiply Zone); `ann.scaleZones` with per-zone `scale` object; lines fully inside use `getEffectiveScaleForLine` / `getLineRealWorldLength` / `getLineLengthForTotals` (then multiply zone factor); `TOOL.SCALE_ZONE`; `scaleZoneBtn`, `scaleZoneBtnSidebar` (Font Awesome scale glyph rotated 180° vs Set Scale); overlap disallowed with other scale zones; requires page scale before draw; Set Scale modal in zone mode; context menu **Edit scale**, Delete; `hitTest` returns `scaleZone` after multiply zones; hidden for viewers
- **Measure tool** — `measureBtn`, `measureBtnSidebar`, `TOOL.MEASURE`: two clicks in PDF space for distance; requires page scale (`showSetScaleFirstToast`); preview uses the same scale crosshair as Set Scale; toast via `formatDistFeetInches`; when both endpoints fall in the same scale zone, `handleCanvasClick` builds a synthetic quick line and uses `getEffectiveScaleForLine` (same path as line lengths); touch uses the same handler; available in view mode
- **Delete Zone tool** — Draw rectangular selection (two-click flow like Multiply Zone); confirmation modal with counts; deletes counters (marker center in rect), quick lines (both endpoints in rect), polylines (first and last point in rect), highlights (center in rect), notes (anchor in rect), multiply zones (center in rect), scale zones (center in rect); `deleteZoneBtn`, `deleteZoneBtnSidebar`, `deleteZoneModal`, `TOOL.DELETE_ZONE`; `collectItemsToDeleteInRect`, `performDeleteZone`, `pointInRect`; dashed red preview rect; hidden for viewers
