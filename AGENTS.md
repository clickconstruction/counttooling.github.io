# ClickCount — Agent Instructions

## Spec Reference

**RECONSTITUTE.md** (if present) is the base spec for behavior and data structures; otherwise see ARCHITECTURE.md. The app includes many extensions; see ARCHITECTURE.md "Features Beyond Spec" for the full list.

## Tech Constraints

- Vanilla HTML, CSS, JavaScript
- No build step; static deployment
- Single-file architecture: HTML + CSS + JS in `index.html`
- report.js loads after index.html and uses globals: `state`, `makeAnnotations`, `ptDist`, `polylineDistance`, `formatDist`, `renderIconHtml`, `quickLineLength`, `getLineLengthPdfPts`, `getLineLengthForTotals`, `getLineRealWorldLength`, `getMultiplyZoneForLine`, `getMergedAnnotationsForPage`; exposes `buildReportHtml`, `printReport`, `getPipeToolingSummary`, `getEmailTextSummary`; both summary functions accept optional `options` with `pageIndices` and `getAnnotations`
- jsPDF for Export PDF; html2canvas for report-to-PDF (Export PDFs)
- Supabase (optional): Auth, projects table (`pdf_path`, `pdf_hash`, `size_bytes`), `pdfs` storage bucket, RPC `list_users_for_admin`, `list_projects_for_admin`, `list_user_activity_for_admin`, `list_user_activity_summary_for_admin`; Edge Functions (`admin-create-user`, `admin-delete-user`, `admin-delete-project`, `admin-list-users`, `invite-to-project`, `get-view-project`); config via `config.js` (see SUPABASE_SETUP.md)
- **Supabase migrations**: When creating or modifying migrations in `supabase/migrations/`, always apply them via the Supabase MCP `apply_migration` tool (name: filename without .sql, query: file contents)

## Navigation

1. **Read ARCHITECTURE.md first** — Contains line ranges, section map, and feature list for index.html
2. **Use grep/semantic search** — For specific features, use the search hints in ARCHITECTURE.md
3. **Prefer targeted reads** — Use `read` with offset/limit when editing a known section instead of loading the full file

## Conventions

- Preserve existing patterns and structure
- Coordinates: annotations in PDF-space; use `canvasToPdf` / `toCanvas` for conversion (toCanvas includes devicePixelRatio)
- Do not remove or rename globals used by report.js
- **Scale is per-page**: `page.scale`; use `getPageScale(pageIdx)` to read; never use `state.scale`
- **Persisted settings** (localStorage): `loadProjectFiltersExpanded` (Load Project modal: filter strip expanded vs search-only); `counterSettings`, `lineTypeSettings` (includes `parallelEndsSize`, `lengthLabelSize`, `snapToHorizontalVertical`, `showOnlyLinesOnCurrentPage`), `legendSettings` (bgOpacity, textOpacity, bgColor, showBorder, legendScale, showResizeHighlight), `multiplyZoneSettings` (showLabelOnZone, defaultMultiplier, labelSize, labelPosition), `gridSettings` (spacing, unit, offsetX, offsetY, opacity, color, lineWidth, lineStyle, majorInterval?, snapToGrid), `showGridOverlay`, `exportSettings`, `recentLineColors`, `iconNames`, `iconOrder`, `customIconPaths` (IndexedDB; in-memory cache, loaded at init; one-time migration from localStorage), `pageScales`, `zoomSettings`, `groupColorDisplay`, `pagesTitlesTruncated`, `hideUnmarkedPagesFromSidebar`, `counterSearch`, `lineTypeSearch`, `linesSearch`, `linesTypeExpanded`, `plumbingModifiers` (includes `iconByType` for type-to-icon mapping; also in user_airboard when Artboard saved), `lineModifiers` (Quick Line Size/Material; also in user_airboard when Artboard saved), `specificPagesIncludeReport`, `bundleHighlightsToPdf`, `bundleNotesToPdf` (in exportSettings), `clickcount-last-project` (last-project restore metadata); `maxZoom` per project in save/load; `groups` per project in save/load; `activeCanvasIdByPage` per project in save/load; include new fields in export/import when adding; `config.example.js` as template; `config.js` committed for production deployment. In-memory (not persisted): `state.pdfBufferSize` (bytes; set when `state.pdfBuffer` is set; used because pdf.js detaches the buffer, making `byteLength` 0); `state.userActivityAllRowsCache`, `state.userActivityViewMode` (admin User Activity modal)
- **Line color modal**: `showLineColorModal(currentColor, onApply)` — used for Counters, Line Types, Groups, Lines; Choose Color modal: Presets (COLORS swatches), color picker, Recent; `applyLineColor(color)`; `state.recentLineColors`; `state.pendingLineColorApply`
- **Supabase**: When enabled, `state.supabaseSession`, `state.isAdmin`, `state.currentProjectId`, `state.currentProjectName`; cloud features hidden when `SUPABASE_ENABLED` is false; PDF uploads limited to 50 MB (Supabase storage limit)
- **Modals**: Project Settings (gear, top right on desktop) — Name / Upload / Save Project to Cloud, Load Project from Cloud, Close Project (hidden when no project), Check out Project / Turn In Project / Force Turn In (admin only for force); **Save Status** — two bells open `saveStatusModal`: in-modal bell (`saveStatusBtn`) in the Project Settings checkout row (visible whenever the checkout section is visible and signed in with a project), and header bell (`saveStatusBtnHeader`, class `header-save-status-btn`) in the header (visible whenever signed in to Supabase; hidden on mobile via CSS); both default gray and add `save-status-bell-attention` (yellow) when `lastCloudSaveAttemptFailed && autoSaveDirty` or `checkoutExpiredNeedsAttention`; modal shows canvas + PDF cloud summary at top and rolling 5-minute `saveStatusLog` activity; while open, `saveStatusModalTickTimer` re-renders content every 5s via `renderSaveStatusModalContent` so "Xs/Xm ago" labels stay live (cleared on close or when modal is no longer `.visible`); autosave/auth sync failures use Save Status, not generic sync toast; edit-session expiry (`CHECKOUT_EXPIRED`, `pushSaveEvent('checkout_expired')`) uses Save Status, not toast; `clearCheckoutExpiredAttention()` on successful Check out or Turn in, Share (add users by email), Add additional PDF pages (when PDF uploaded), Download PDF (when PDF in project), Manage Projects (admin), Clear Page (when PDF), Macros (yellow link, below Clear Page, above Advanced), Advanced section (collapsed by default) with Load test PDF (localhost only), Manage Icons, Export Canvas (when markup, editor only), Export PDF (when PDF in project; both available in view mode), Import Canvas (hidden in view mode), Canvas Repair (when PDF), Empty cache and hard reload (red danger; clears IndexedDB and localStorage, reloads); User Settings (user icon) — email, password, Artboard (Save, Load from Cloud, Export, Clear Artboard; counters, line types, plumbingModifiers, lineModifiers to user profile); mySettingsClearAirboard, Add User / Manage User (admin), All Users (admin), Sign Out; Manage User modal — header with yellow activity icon (`manageUserModalAllActivityBtn`) opening **User Activity** in all-users mode; list users with Delete per row; per-row activity icon opens single-user activity; Manage Projects modal — list all projects with Delete per row and Force turn-in (admin) per checked-out row; project name on top, date/time/size below, "Canvas only" badge for projects without PDF; **User Activity modal** (`userActivityModal`) — admin; **Events** vs **Summary** (all-users mode); Events: `list_user_activity_for_admin` + `list_users_for_admin`, toolbar (user dropdown, filter on `state.userActivityAllRowsCache`), `state.userActivityViewMode`; event **Time** and single-user rows use `formatUserActivityDateTime` (**America/Chicago**); Summary: `list_user_activity_summary_for_admin`, last sign-in via `formatLastSignInUserActivity` (calendar Today/Yesterday in Chicago); Manage/All Users lists still use `formatLastSignIn` (browser-local); groupAssignModal (Assign to group) with + Add group opening groupModal (Add/Edit Group; z-index 210 so it stacks above); Export PDFs modal — thumbnails, per-page marked/unmarked/exclude, bulk actions, Include takeoff report / Bundle highlights / Bundle notes toggle switches with "— none to show" and disabled when no data; loadAnnotationsModal — "You have saved annotations for this PDF. Load them?" with project list; shown on PDF upload when hash matches cloud projects; viewLinkEmailModal — "Enter your email to view these plans" for view-only share links; saveBeforeLoadModal — unsaved changes prompt (Save now / Don't Save / Cancel); lastSessionRestoreModal — on boot when `last.projectId` exists (no longer requires `last.pdfHash`); local backup checked first: when IndexedDB has backup under `'local'` with pdfBlob and data, offers "You have a local session from your last visit" (Load test PDF → Open without save); cloud project restore offered when no local backup; for local restore, proj.id is `'local'`, `state.currentProjectId` set to null, `cachedBlob` passed from backup; uses `proj.pdf_hash || last.pdfHash` for cache lookup; Supabase restore uses `download()` with empty-blob check; `hasIdbPdf` requires `pdfBlob.size > 0`; prompts Keep / Discard before restore; Discard clears local state and deletes project from cloud if owner; saveProjectModal — contents list shows Canvas (included) and PDF with size in MB; size from `state.pdfBufferSize` (local) or pdfCache/storage.info (cloud); fallback to PDF section when `state.pages.length > 0`; "Canvas only" when no PDF; pipeToolingCopiedModal — "Copied to clipboard" toast (1.5s auto-dismiss); noteModal — Add/Edit Note (textarea, Cancel/Done); multiplyZoneModal — Multiply Zone: preview "In this area: X counter(s), Y line run(s) (Z ft)", multiplier input (min 1, default 2), Cancel/Apply; deleteZoneModal — Delete Zone: preview "In this area: X counter(s), Y line run(s), …", Cancel/Delete (confirm before delete); multiplyZoneSettingsModal — Multiply Zone Settings: Show label on zones toggle, Default multiplier, Label size (8–24), Label position; opened by right-clicking multiply zone icon in header or sidebar; openMultiplyZoneSettingsModal; scaleModal — tabs Select two points, Architectural & Engineering (presets); Custom scale at bottom (fraction e.g. 1/4 or 0.25, feet, Apply); when `scaleModalApplyTarget === 'zone'` (after drawing a Scale Zone rectangle or context **Edit scale**), Apply writes `ann.scaleZones[].scale` instead of `page.scale`; `pendingScaleZone`, `pendingScaleZoneEdit`; `resetScaleModalZoneMode`; full-width; parseFraction; chooseLineTypeModal — tabs Choose | Create | Quick (like Counter modal); search; Shift+L for Quick tab; L always opens modal; Snap-to-H/V toggle in header (right of Polyline button; visible when Line or Polyline tool selected); also in Line Type Settings; J hotkey; macrosModal — Keyboard Shortcuts (M/S/C/L/J/P/D/H/X/N/R/Esc/arrows/Enter/Ctrl+Z/Ctrl+Shift+Z/Ctrl+R; Shift+C Quick tab when Counter modal open; Shift+L Quick tab when Line modal open; X = Multiply Zone; Scale Zone = rotated Set Scale icon in header/sidebar), opened via Project Settings Macros link; customIconTipsModal — Custom Icon Tips (what makes a great icon for upload); opened by clicking "Custom Icons" label in Create Counter or Counter Details; counterLineTypeDetailsModal — opened via edit pen on Counter or Line Type row; Name (editable), Color (swatch), On pages (clickable jump), Delete (count=0 immediate; count>0 confirm + remove markers/lines); linePropertiesModal — Line Properties: read-only **Line type:** line (`#linePropertiesLineType`, centered; from `state.lineTypes` / `lineTypeId`, or em dash if missing); Name, Color, Start drop, End drop, +1/+10/-10/-1/Clear buttons, Edit vertices for polylines; opened via edit pen on individual line in Lines dropdown or context menu "Line Properties"; plumModal — Quick Plumbing: PLUM buttons hidden in Counters and Lines sections (`.sidebar-plum-row { display: none }`); plumModal still exists for programmatic use; Name at top, Add Counter in header right; Size, Type (icon box for type-to-icon mapping), Material; Icon tabs (Icon/Custom Icons); 3 rows visible in icon grid; `getPlumbingModifiers`, `savePlumbingModifiers`, `PLUMBING_DEFAULTS` (sizes 0.5in format); summaryCountDetailModal — "— by page" breakdown with thumbnails when clicking count/line in sidebar Summary; legendSettingsModal — Summary Legend: background opacity, text opacity, background color, Show border, Legend size (50–400%), Highlight resize area; opened by clicking Summary header; gridSettingsModal — Grid overlay: spacing, unit (ft/in/m/cm/yd); origin row (Set origin on page, Snap counters to grid toggle); Line color, Major line every N lines; Line opacity and Line width in one row; line style (solid/dashed/dotted) centered; opened when grid button clicked and grid is off; Apply enables grid; `resetGridOrigin()` on new document load; drawGrid draws lines left/above and right/below origin; zoomModal — Zoom Settings (max zoom per project, mousewheel speed universal), opened via zoom percentage click; Manage Icons — Custom icons section with Edit (toggle edit mode), select rows, Delete selected; preparePdfModal — Prepare PDF for Cloud (Add additional PDF pages): Project name, Page name (collapsible dropdown minimized by default, icon ▶ at end of "Page name", whole line clickable), Prev/Next, Delete, Rotate (90° per page), Undo; left: Cancel, Open; right: Download Trimmed PDF, Save and Open; Open (left) runs and awaits `writeTakeoffStateBackup()` immediately after commit; per-page file size (This page / Total) when computed; commitPreparePdfToState; authModal — Sign In with "Sign in as test user" (localhost only, when DEV_AUTH_EMAIL/DEV_AUTH_PASSWORD in config)
- **Summary legend overlay** — `state.showLegendOverlay` (default true); toggled via legend button in header/sidebar; overlay on canvas with counters and line totals; draggable (header bar); resizable from bottom-right grip (16×16 PDF-unit hit area); Legend Settings modal for appearance; legend in annotations: `{ x, y, w, h, userResized? }`; `userResized` set when user manually resizes (drawLegend preserves size); resize/drag use window mousemove and mouseup so drag continues when cursor leaves canvas; mouseleave does not clear `resizingLegend`/`draggingLegend`
- **Export dropdown** — `#exportDropdown` in header; 28×28 cloud icons (aligned with gear, Share, print); cloud-upload when editor has no pages (click fires `#pdfInput` — no import submenu); cloud-download opens export menu when pages exist (Export Canvas/Both gated by `projectHasAnyCanvasMarkup()`, Export PDF when PDF in project; **Import Canvas** in menu when editor, PDF, no markup, opens `#importInput`; menu hidden when no rows); Advanced **Export Canvas** and sidebar Import still available; `#exportBtn` no-ops without markup; viewer with no pages: control hidden
- **Export Options** — Yellow section title above Export PDFs in sidebar (`#exportOptionsSectionTitle`); Copy to /Tooling is the yellow (primary) button; Copy Summary (Email/Text) dropdown (`#copySummaryTextDropdown`) with same canvas options; both dropdowns: This Canvas Only (hidden when single page), All Visible Canvases, All Canvases; Copy Summary menu pops up when no space below
- **Download current page** — `#downloadCurrentPageBtn` in header (far right); yellow printer icon (Font Awesome–style SVG, 28×28); dropdown when multiple canvases or multiple pages: "Print This Canvas Only" (active canvas), "Print All Canvases on Page" (one PDF page per canvas with caption), "Print All Plan Pages and their Current Canvas" (all pages, active canvas each), "Print All Pages & Canvases" (all pages, all canvases with captions); single page and single canvas: direct download; visible in edit and view mode when PDF loaded; `downloadCurrentPageAsPdf(mode)`; uses `renderAnnotationsToContext`, jsPDF; filenames `takeoff-page{N}_{projectName}.pdf`, `takeoff-page{N}_all-canvases_{projectName}.pdf`, `takeoff-all-pages_{projectName}.pdf`, `takeoff-all-pages-canvases_{projectName}.pdf`
- **Download PDF** — Project Settings "Download PDF" downloads the current project's PDF as-is; Prepare PDF modal "Download Trimmed PDF" (right side) downloads the trimmed PDF (kept pages only). Rotation applied in Prepare PDF is stored in state.pages on commit; `downloadProjectPdf()`, `downloadPdfBuffer()`
- **Show Report** — Dropdown (`#showReportDropdown`) with four options: Takeoff Report for this Canvas Only, Takeoff Report for all Canvases on Page (hidden when single canvas), Takeoff Report for all Plan Pages and their Current Canvas, Takeoff Report for all Pages and Canvases; each opens report in new tab via `printReport(mode)`; `buildReportHtml(options)` supports `pageIndices` and `getAnnotations`; hidden when no counts or lines
- **Export PDFs** — Modal (`#specificPagesModal`): marker size and line width sliders (25–150%), Include takeoff report, Bundle highlights, Bundle notes toggles; page thumbnails with marked/unmarked/exclude; bulk actions All Marked Up / All Not Marked Up / Exclude All; Download exports selected pages; `downloadSpecificPages()`; uses `state.exportSettings.markerScale`, `state.exportSettings.lineScale`; hidden when no counts or lines
- **Copy to PipeTooling** — Dropdown (`#forPipeToolingDropdown`) below Export PDFs; yellow (primary) button; drop-up menu (always opens above); options: This Canvas Only (current page, active canvas; hidden when single page), All Visible Canvases (all pages, active canvas), All Canvases (all pages, merged); copies tab-delimited summary via `getPipeToolingSummary(options)`; counters as name/count/pages; line types as `[unit] of [name]`/length/pages; hidden when no counts or lines; sidebar links use https://pipetooling.com
- **Show Highlights / Show Notes** — Buttons open highlights or notes summary in new tab (not download); hidden when no highlights or no notes respectively; toggle switches in Export PDFs modal for bundling to PDF
- **Hotkeys** — M (Move), S (Set Scale), C (Counter), L (Line modal), J (Snap to horizontal/vertical), P (Polyline), D (Measure Distance — two-click; distance toast uses that Scale Zone’s scale when both clicks fall in the same zone, otherwise page scale), H (Highlight), X (Multiply Zone), N (Note), R (Rotate page); Shift+C = Quick tab when Counter modal open; Shift+L = Quick tab when Line modal open; arrow keys: Left/Right = page nav, Shift+Left/Right = previous/next marked page, Up/Down = switch canvas layers (when multiple canvases); Ctrl+Z / Ctrl+Shift+Z for Undo/Redo; Ctrl+R / Cmd+R (Refresh); ignored when focus is in input/textarea/contenteditable
- **Counter settings** — Ring section only visible when "Show ring around counters" toggle on; Show ring and Solid ring use toggle switches; Solid ring default is true; Show only counters on current page filters sidebar to counters with markers on the current page
- **Line type settings** — Show only line types on current page filters sidebar to line types with lines on the current page
- **Mobile header slot** — When no PDF: Upload PDF in header (transparent on mobile, text only); when PDF loaded: Set Scale (until scale set); `body.has-pdf` toggled in updateUI
- **Status bar** — Dual indicators: circle (dot) = canvas sync, square = PDF sync; each has a label next to it (e.g. "Canvas Synced with Cloud", "PDF Synced with Cloud", "PDF No PDF in project"); both use red/yellow/green/grey; messages: "Project not saved to cloud", "Upload PDF to start a project" (no PDF); Name / Upload / Save Project to Cloud / Load Project from Cloud; save progress shown during upload
- **Pages badges** — `badge-scale-set` = yellow number when scale set; `badge-has-ann` = yellow outline when page has counts, lines, notes, highlights, multiply zones, or scale zones
- **Page rotation** — Per-page `page.rotation` (0/90/180/270); rotate button (↻) in zoom bar; `rotatePage90()` transforms annotations; notes text rotates with page; persisted in save/load
- **Counter/Line Type row** — Row click selects for placing; edit pen (✎) opens counterLineTypeDetailsModal (Name, Color, On pages, Delete)
- **Line drops** — Per-line `startDrop` and `endDrop` (in page scale units) for vertical runs; X markers at endpoints when drop > 0; included in total length via `getLineLengthPdfPts`; Line Properties modal (edit pen or context menu) shows source line type name, then Name, Color, Start drop, End drop, +1/+10/-10/-1/Clear buttons; sidebar Lines list shows "5 + 3 ft" below badge when drops; persisted in annotations
- **Canvas context menu** — `#contextMenu`; `handleContextMenu` → `hitTest` → `state.ctxTarget`; `showContextMenu` toggles actions by type (Edit note, Line Properties, Show Length, Assign to group, Edit multiplier/scale, Delete); below **Delete**, `#ctxTargetNameRow` (`.context-menu-target-name`) shows counter name for markers (`state.counters` / `typeId`) or line type name for quick lines and polylines (`state.lineTypes` / `lineTypeId`, same idea as `#linePropertiesLineType`); row hidden for other hit types; long-press on touch also opens menu; not available in view mode (`handleContextMenu` returns early)
- **Line types** — Have `curveStyle: 'straight' | 'arc'` (default `'straight'`); when `arc`, quick lines render as quadratic Bezier arcs and use arc length for totals; polylines remain straight; include `curveStyle` in save/load and export/import
- **Marked page navigation** — ‹‹ and ›› buttons outside page nav; jump to previous/next page with annotations; yellow text and border when selectable
- **Auto-save** — Every 5 seconds when dirty: signed-in to Supabase, unsigned to localStorage; 5-second localStorage backup for all users; `markProjectDirty()`, `performAutoSave()`, `autoSaveDirty`, `lastSaveIncludedPdf`, `savePdfInProgress`; **Save status** — in-modal bell `saveStatusBtn` and header bell `saveStatusBtnHeader` (`.header-save-status-btn`); both always visible when signed in (header hidden on mobile), default gray, `save-status-bell-attention` (yellow) shared by both on sync failure or checkout expiry; in-memory `saveStatusLog` (300s), `pushSaveEvent`, `lastCloudSaveAttemptFailed`, `checkoutExpiredNeedsAttention`; `saveStatusModal` live-ticks via `saveStatusModalTickTimer` + `renderSaveStatusModalContent` every 5s while open to refresh ago labels (cleared on close); sync/auth failures surface via Save Status bell (not generic toast); checkout/session expiry surfaces via Save Status **and** a one-shot toast (`CHECKOUT_EXPIRED_TOAST_MSG`, gated by `checkoutExpiredToastShown`); **Checkout keep-alive / lock probe** — `probeCheckoutLock(runId)` (server-side truth via `refresh_checkout_activity` RPC); `checkoutKeepalive` runs every `CHECKOUT_KEEPALIVE_MS` (10 min) when visible and checked out; `CHECKOUT_NEAR_EXPIRY_MS` (5 min) and `CHECKOUT_SOFT_GRACE_MS` (60 s) drive the 25-min probe boundary and the >31-min hard-skew shortcut in `performAutoSave` and the SaveProject modal; client no longer calls `check_in_project` voluntarily; on confirmed expiry `suspendAutoSaveUntilCheckout` halts the autosave loop until the user re-checks-out (cleared by `clearCheckoutExpiredAttention()` which also clears the toast flag); PDF IndexedDB cache on save; last-project restore from `clickcount-last-project` — on boot prompts Keep/Discard via `lastSessionRestoreModal` before restoring; Discard clears local state and deletes project from cloud if owner; save-before-load prompt when loading with unsaved changes; `withTimeout` wraps Supabase calls in performAutoSave and doTurnIn to prevent hang; `takeoff-state` includes `projectId` and `lastModifiedAt` when a project is loaded; last-project restore prefers takeoff-state backup when it is newer than Supabase; `autoSaveDirty` restored when performAutoSave fails so Turn in retries; `performAutoSave` acquires `saveInProgress` in try/finally so concurrent autosave + manual save serialize; `doTurnIn`, `saveBeforeLoadSave`, and `performSaveProjectToCloud` clear `autoSaveDirty` BEFORE awaiting and restore on failure so edits made during the save are not wiped; `performSaveProjectToCloud` on failure sets `lastCloudSaveAttemptFailed = true` and calls `updateSaveStatusIndicator()` so both bells turn yellow; `saveBeforeLoadSave` opens `openLoadProjectModal()` on success when no `pendingCopyProject` (regular Load flow); PDF upload (`pdfInput`), Prepare PDF (`commitPreparePdfToState` callers), and Import Canvas (`importInput`) call `markProjectDirty` so cloud reflects them; `performSaveProjectToCloud` recovers a detached `state.pdfBuffer` via `pdfCacheGet(currentProjectId, pdfHash)` and logs `pushSaveEvent('manual_save_recover', …)`, otherwise (no cache and no `pdfStoragePath`) returns a clear error instead of silently skipping the upload; `withTimeout` wraps every Supabase call in `performSaveProjectToCloud` (10 s `pdf_hash` select / `storage.info`, 5 s `pdfCachePut`, 30–60 s inserts/updates/uploads); local `saveWithTimeout` helper removed; `takeoffBackupPut` reads the existing IDB record first and skips/aborts when the incoming `lastModifiedAt` is older (`saveDebugLog('takeoffBackup.skip_stale', …)`); custom-icon upload (`customIconUploadInput.onchange`) calls `markProjectDirty` after `saveUserCustomIcons` so the next autosave picks up new icons; PDF merge (`pdfInput.onchange`) re-checks the 50 MB ceiling by summing `buffersForMerge` byte sizes before `mergePdfBuffers` and reverts `state.pages` to `startPageIdx` + prunes `activeCanvasIdByPage` + alerts when the projected size exceeds `PDF_MAX_SIZE_BYTES`; manual-save `pdfCachePut` failure surfaces a one-shot `pushSaveEvent('manual_save_cache_warn', …)` gated by `pdfCacheWarnShown` (reset on sign-out) so a detached-buffer recovery risk is visible in the Save Status modal without log spam; `subscribeToProjectCheckoutChanges` uses `.subscribe((status, err) => …)` with capped backoff reconnect (`projectsCheckoutReconnectTimer`, `PROJECTS_CHECKOUT_RECONNECT_BACKOFF_MS` = 1s/3s/10s/30s) and forces one `refreshProjectPermissions` on `SUBSCRIBED`; force-turn-in path in `refreshProjectPermissions` fires a best-effort flush before flipping to viewer, logs `pushSaveEvent('force_turn_in', …)`, and the toast warns when there were unsaved edits; checkout RPCs (`check_out_project`, `refresh_checkout_activity`) return `server_now` and `checked_out_at` (migration 038), client tracks `serverClockOffsetMs` via `updateServerClockFromRpc` and uses `serverNowMs()` for all expiry math so clock skew on the user's machine never produces false "edit session expired" toasts; `writeTakeoffStateBackup()` called before Turn in; backup fetches `proj.pdf_hash` from Supabase when `last.pdfHash` is null, so IndexedDB backup can populate PDF from cache for last-project scenarios; `writeTakeoffStateBackup` is async; runs on visibilitychange (hidden) to capture state before tab close; debounced 1s after `markProjectDirty`; 5s interval; `commitPreparePdfToState` passes `trimmedBuf.slice(0)` to `pdfjsLib.getDocument()` so buffer stays intact for save/backup
- **Undo/Redo** — Last 5 moves in local memory; `undoStack`, `redoStack`, `pushUndoSnapshot()`, `clearUndoStacks()`; Undo/Redo buttons in bottom bar next to rotate; Ctrl+Z / Ctrl+Shift+Z; cleared on load, project switch, or when viewer
- **Middle mouse pan** — Hold middle mouse button to pan regardless of active tool; `state.isPanning`, `state.panStart`; move SVG cursor during pan; window mouseup releases when released outside canvas
- **Sharing** — `project_shares` table; checkout/turn-in (one editor at a time); 30-minute inactivity expiry (lock extends on edits/saves via `refresh_checkout_activity`, plus an idle keep-alive `checkoutKeepalive` every `CHECKOUT_KEEPALIVE_MS` while the tab is visible and the user holds the lock; `visibilitychange` returning to visible runs `probeCheckoutLock` before `refreshProjectPermissions`; `visibilitychange` to hidden fires a best-effort `performAutoSave` flush when dirty); system admin can force turn-in and check out any project (migration 029); `state.isViewer`, `state.canCheckOut`, `state.checkedOutBy`, `state.checkedOutEmail`; Load Project uses `list_accessible_projects` RPC (returns `owner_email`, `my_access_role` for filters — migration 036); Share modal adds users via `invite-to-project` Edge Function; when admin force turns in, user with project open gets realtime notification ("Project was turned in. You can check out to edit again.") and UI switches to view mode via `refreshProjectPermissions`; Manage Projects modal has Force turn-in (admin) per checked-out row; `list_projects_for_admin` returns `checked_out_by`, `checked_out_at`, `checked_out_email` (migration 025)
- **View links** — `project_view_links`, `view_link_access_log`; Share modal "View links" section: create, list, copy URL, access log, revoke; URL `?t=TOKEN`; `get-view-project` Edge Function (no JWT); email domain gate (clickplumbing.com); `viewCacheGet`/`viewCachePut` for IndexedDB; `viewLinkEmailModal`; `initViewOnlyMode(viewToken)` on boot when `?t=` present; Copy view link button (`#copyViewLinkBtn`) left of gear copies most recent view link (creates if none); create restricted to owners/editors — copyViewLinkBtn and shareViewLinkCreate hidden when `state.isViewer`
- **Canvas browsing in View mode** — Viewers can switch active canvas via pills and layers dropdown; change is local (no markProjectDirty); Add canvas and Edit canvas remain restricted
- **Toggle switches** — `.toggle-switch` with `.toggle-switch-knob`; button toggles hidden checkbox, syncs `aria-pressed`, dispatches change; used for Show group colors, Counter Settings (Show ring, Solid ring), Save Project Include PDF, Export PDFs (Bundle highlights, Bundle notes, Include report)
- **Counter button dynamic icon** — When counter tool selected and a counter chosen in sidebar, `counterBtn` and `counterBtnSidebar` show that counter's icon and color (with black outline) instead of default ellipse; `COUNTER_BTN_DEFAULT_SVG`; `headerActiveCounter` cleared
- **Custom icon upload** — Create Counter and Counter Details modals: Custom Icons grid has "+ Upload" cell; hidden `#customIconUploadInput` (accept .svg); `parseUploadedSvg`, `getUserCustomIcons`, `saveUserCustomIcons`, `getEffectiveCustomIcons`; user icons in IndexedDB `customIconPaths` (in-memory cache); included in export/import; click "Custom Icons" label to open customIconTipsModal with upload tips; Manage Icons modal Custom icons section: Edit toggles edit mode, select rows, Delete selected removes from IndexedDB
- **Bundled custom icons** — SVGs in `my-counters/` → run `npm run build:icons` → paste output into `CUSTOM_ICONS` in index.html; see CUSTOM_ICONS.md
- **Sidebar collapse** — Clicking the collapse icon (▼/▶) or the black space next to it minimizes the section; `.collapse-icon` in sidebar has expanded padding for larger hit area; Groups and Lines sections start minimized by default
- **Multiply Zone tool** — Draw rectangular zone; apply multiplier to counts and line lengths for items whose start/end fall inside; `ann.multiplyZones`; first containing zone wins when overlapping; two-click flow; multiplyZoneModal (preview, multiplier input); context menu Edit multiplier, Delete; right-click multiply zone icon (header/sidebar) opens multiplyZoneSettingsModal; openMultiplyZoneSettingsModal; X hotkey; hidden for viewers
- **Scale Zone tool** — Two-click rectangle (like Multiply Zone); `ann.scaleZones` with per-zone `scale` object; lines fully inside use `getEffectiveScaleForLine` / `getLineRealWorldLength` / `getLineLengthForTotals` (then multiply zone factor); `TOOL.SCALE_ZONE`; `scaleZoneBtn`, `scaleZoneBtnSidebar` (Font Awesome scale glyph rotated 180° vs Set Scale); overlap disallowed with other scale zones; requires page scale before draw; Set Scale modal in zone mode; context menu **Edit scale**, Delete; `hitTest` returns `scaleZone` after multiply zones; hidden for viewers
- **Measure tool** — `measureBtn`, `measureBtnSidebar`, `TOOL.MEASURE`: two clicks in PDF space for distance; requires page scale (`showSetScaleFirstToast`); preview uses the same scale crosshair as Set Scale; toast via `formatDistFeetInches`; when both endpoints fall in the same scale zone, `handleCanvasClick` builds a synthetic quick line and uses `getEffectiveScaleForLine` (same path as line lengths); touch uses the same handler; available in view mode
- **Delete Zone tool** — Draw rectangular selection (two-click flow like Multiply Zone); confirmation modal with counts; deletes counters (marker center in rect), quick lines (both endpoints in rect), polylines (first and last point in rect), highlights (center in rect), notes (anchor in rect), multiply zones (center in rect), scale zones (center in rect); `deleteZoneBtn`, `deleteZoneBtnSidebar`, `deleteZoneModal`, `TOOL.DELETE_ZONE`; `collectItemsToDeleteInRect`, `performDeleteZone`, `pointInRect`; dashed red preview rect; hidden for viewers
