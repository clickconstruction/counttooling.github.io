# Handoff — 2026-08-20 batch session (Robert → Will)

Robert paused this session to conserve credits. This doc is the single entry
point for picking the work up. Nothing from today is merged to `main` — every
deliverable sits on a branch, gated and green, awaiting your review.

## The one branch that matters

**`claude/journey-batch-final`** — built on current `origin/main` (post your
Ghost/Stamp merge `c1ad892`), carrying everything below. Gates at commit
`2188bc9`+: `npm run check` 8/8, `node --test` 312/0, the 39 specs covering
every judge-adjudicated surface green on a verified private server; the full
Playwright suite result is in the final session report.

What it contains, in merge order:
1. **The f5 batch** (13 items): render-worker teardown fix, Clear Page
   reachable signed-out, counter-create cluster, icon-upload reveal,
   multiply-adjusted counts, dblclick sheet rename, hidemarks hitTest guard,
   copy cluster, prepare-PDF thumbnail-grid trim, 7 guide-article fixes,
   Clear Page confirm copy, signed-out surface audit.
2. **Batch 3** (16 lanes): #15 toast rework (non-blocking corner stack,
   z500, Distance→footer chip), #16 Quick Count per-Type icons, B2 intake
   feedback (incl. a silent palette-wipe fix), B13 "My Standards" naming,
   B6 viewer-trim (plan names, email-gate card, allowed-domains), B5 PDF
   pagination (row-boundary cuts; also fixed a ~9% horizontal stretch),
   X2 mobile layers peek, Tier-4 guides complete (G1–G11), three drift-walk
   dossier addenda, four fork adjudications.
3. **The four judge merge recipes applied** (see below).

## History preserved for comparison (do not delete until reviewed)

- `claude/journey-batch-integration` — the ORIGINAL (pre-redo) batch, its own
  integration, full suite 271/0. The per-item branches `claude/<slug>`.
- `claude/f5-journey-batch-integration` — the clean-room redo, suite 269/0.
  Per-item branches `claude/f5-<slug>`.
- Batch-3 branches `claude/f5b3-*`.

## The fork adjudications (empirical, all four in the final branch)

| Fork | Verdict | Why |
|---|---|---|
| #24 counts | original + f5's tooltip + a footnote-scope fix | f5's inline badge collapses names to 0px at the app's only sidebar width (220px) |
| render-worker | f5's serialization + original's gen-stamped protocol + its tests | each had a hole the other closed; stale results can no longer blit or trip failWorker |
| copy resume | f5's chip + hardening | 1 tap vs 2–3, visible at the decisive moment; chip repositioned off the page-nav, dismissable, project-guarded |
| counter prefill | original outright | f5 re-shipped the "searched Sink, shipped Water Closet" bug; name-follows-icon + suffix toast win |

## Decisions waiting on you

1. **Merge `claude/journey-batch-final` to `main`?** That is the whole ask.
   After merging: flip the JOURNEY-MAP status rows for #13, #16–#19, #24–#27,
   B2, B3(-chooser), B5, B6(half), B13, B14(one item), B17(counter half),
   X2, G1–G11 — the standing rule is rows flip on MERGE, so none were flipped.
2. **Known regression, shipped-with-flag:** the counter modal's tab strip sits
   under the header at viewport heights ≤~730px (1280×720 laptops) — caused by
   un-hiding the icon search row (#18), present in BOTH batches' versions.
   Same fix recipe as the Tier-1 Set Scale clip (max-height + scroll). Top of
   the next wave. (J4 addendum, finding 14.)
3. **Deploy watch after merge:** `render_worker_fallback` should go silent —
   the 23-events/6-users teardown race is fixed. If it keeps firing, the fix
   missed a path.
4. Your gate decision (desktop settings ungated) landed mid-batch; all guide
   text and JOURNEY-MAP corrections were reconciled to it. The audit doc
   (`journeys/_signed-out-surface-audit.md`) remains the record; its one real
   caution stands: **Empty cache and hard reload** now sits two taps from any
   signed-out user on every width and destroys local work + backups by design.
   Consider a stronger confirm there.

## Next-wave shortlist (evidence-ranked, from the walkers)

1. Counter-modal clip regression (above).
2. J2/N4: Escape in the grid's zoom view discards the whole trim session —
   fold into B15's confirm-before-discard.
3. Hide-marks + armed placement tools: placing invisible marks is possible
   (J9 addendum) — decide gate-or-allow once.
4. The remaining app.js Tier-2 items now unblocked by the toast rework:
   #20 #21 #22 #23 #28, B1 Esc-ladder.
5. B6's four ready-to-apply one-liners (exact edits in the viewer-trim lane
   report; also `features/copy-project.js:121` has the third `document.pdf`
   hardcode, and `applyPageAnnotationsFromData` drops owner page renames on
   every cloud load — filed, unfixed).
6. `scripts/build-screenshots.js` still shoots the OLD prepare-PDF modal with
   now-orphaned callouts — update before the next `build:screenshots` run;
   `how-to-do-a-pdf-takeoff.md` still embeds the stale screenshot.

## Operational lessons (baked into memory for future sessions)

Parallel agent lanes on this repo need: unique ports + curl sentinels (never
3456 — `reuseExistingServer` trap), a gitignored `config.local.js` stub, and
explicit base pinning + ancestry checks (worktrees can fork from another
session's lineage — it happened twice today).

Program state: `_NEXT.md` Stage 1–2 done (telemetry 2026-08-20 + the executed
re-rank); Stage 4 (Tier-3 remainder), Stage 5 (cloud-interior walk — needs
your staging-account decision), Stage 6 (Tier-5 product session) open.
