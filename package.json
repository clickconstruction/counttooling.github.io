{
  "name": "counttooling",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:delete-page": "playwright test delete-page.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
